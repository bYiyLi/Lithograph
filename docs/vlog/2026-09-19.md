# 2026-09-19

Timezone: UTC

## 04:50 — Phase 13 review and hardening

- Continued Phase 13 Managed Semantic Vector implementation and review against the current repository design and acceptance plan.
- Clarified the persistent embedding-cache contract: `maxBytes` is always positive; disabling the cache preserves existing rows, while enabled configuration enforces the configured FIFO byte budget.
- Fixed a concurrency gap in persistent embedding-cache publication by reading the operational cache policy inside the publication SAVEPOINT, preventing a completed concurrent disable or budget change from being bypassed by a stale pre-transaction policy snapshot.
- Fixed Phase 13 HNSW test callers after the persistence API gained a cancellation callback, and closed the associated rustfmt/all-target clippy finding.
- Targeted verification passed: embedding-cache tests 8/8, embedding-provider tests 5/5, OpenAI-compatible provider tests 20/20.
- `cargo make quality` passed in the repository's pinned quality environment. Coverage remained regions/functions/lines 83.32% / 84.14% / 85.11%, and applicable inherited openCypher TCK remained 3,777/3,777.
- `scripts/ci.sh` passed after a fresh rerun with a longer connector execution window. Phase 13 full integration passed on SQLite 3.45.0 and 3.53.4, including OpenAI-compatible and synthetic provider loading, semantic schema/query/rebuild/cache/version-publication checks, concurrency/WAL checks, and semantic performance probes.
- Concurrency evidence: provider sleep 1000 ms, writer wait 0 ms, non-autocommit provider calls 0, persistent cache entries 2. Warm-query/rebuild performance evidence showed zero provider calls after cache population.
- An overlapping Nexum/Cargo session was observed during earlier verification; it was allowed to finish without intervention, and final quality/CI evidence was rerun on a stable worktree afterward.
- Local Phase 13 acceptance remains closed except SV13-27 hosted six-target Release Matrix execution. Phase status remains `in_progress`; no commit or push was performed.

## 06:11 — Phase 13 OpenAI-compatible transport security closure

- Continued the authorized Phase 13 implementation/review loop; no commit, push, release, or deployment was authorized or performed.
- Review found that the HTTP dependency's TRACE path can expose wire-level data. The standalone OpenAI-compatible Provider now compiles its dependency `log` graph with static max level `Off`, verifies that invariant during extension initialization, and remains a pure `cdylib`. Release Matrix and source-build instructions now build Lithograph and the Provider in separate Cargo invocations so the Provider-specific feature cannot leak into the main extension artifact.
- Review of the locked redirect implementation found that nonstandard custom headers can survive redirects. The Provider now sets `max_redirects=0`; a regression verifies a 302 with a custom `X-API-Key` is not followed and the secret is not echoed.
- Review found ureq's default environment proxy discovery was a hidden routing input outside versioned `providerConfig`. The Provider now sets `proxy(None)`; `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` are not implicit Provider configuration.
- The cache-policy publication race fix from the preceding review remains intact: persistent publish reads the current operational policy only after entering its write SAVEPOINT.
- Targeted Provider verification passed: OpenAI-compatible 22/22; embedding ABI 5/5; real Provider artifact inspection and Homebrew-SQLite `.load` smoke passed; separated build dependency inspection confirmed Lithograph itself does not inherit the Provider's static log-off feature.
- Fresh `cargo make quality` passed after all transport changes. Coverage regions/functions/lines is **83.48% / 84.24% / 85.26%**; Phase 13 real-SQLite integration passed and applicable inherited openCypher TCK remains 3,777/3,777.
- Fresh `scripts/ci.sh` passed after all transport changes. SQLite 3.45.0 and 3.53.4 both passed dual-extension Phase 13 checks, Native/FTS integration, Provider smokes and concurrency/performance gates. With 1000 ms synthetic Provider wait, writer wait was 0 ms and writer hold 5 ms; the performance fixture retained 16 unique provider inputs for 64 owners, then 0 provider inputs for same-connection/new-connection warm queries and warm rebuild.
- Final local completion boundary is unchanged: SV13-27 still lacks the six hosted Linux/macOS/Windows x64/arm64 Release Matrix runs for this uncommitted worktree. Phase 13 therefore remains `in_progress`.

## 07:46 — Phase 13 read-only, integrity and transaction-boundary closure

- Continued the Phase-level review loop and found three contract gaps: read-only rebuild was rejected even though TEMP-only rebuild is designed to work; format4 integrity did not scan persistent embedding payloads; and a Semantic Provider call could be composed with graph mutation in one Cypher execution.
- Read-only rebuild now completes through TEMP embedding/HNSW state, revalidates the pinned Semantic definition, and skips only the optional persistent-cache publication. The real SQLite probe verifies Provider execution succeeds while `main._lithograph_embedding_cache` remains unchanged.
- Integrity now validates embedding cache hash/blob types and lengths, nonnegative byte accounting, supported dimensions, FLOAT32 coordinate type, payload size and finite coordinates. Corruption fixtures cover accounting, vector length and non-finite payload failures.
- Planner validation now rejects Semantic query/maintenance combined with graph mutation or transaction-owning subqueries before Provider I/O with `TRANSACTION_BOUNDARY_REQUIRED`. The real extension regression proves `embed=0`, no Audit node and no Branch-head move.
- The first quality rerun exposed a hard file-budget overflow caused by the new regression test; the test moved into the existing mutation test module. The next rerun exposed CCN 29 in the cache-integrity scanner; field validators were split into focused helpers without changing finding codes or semantics.
- Final `scripts/ci.sh` and `cargo make quality` both exited 0. SQLite 3.45.0 and 3.53.4 passed all 18 Phase 13 real-extension checks; coverage regions/functions/lines is **83.47% / 84.27% / 85.25%**, and inherited openCypher TCK remains 3,777/3,777.
- SV13-27 still requires the hosted Linux/macOS/Windows x64/arm64 Release Matrix against a committed/pushed revision. No commit or push was performed, so Phase 13 remains `in_progress`.

## 08:13 — Phase 13 delivery authorization

- The user explicitly authorized committing and pushing the current Phase 13 changes to `main`, then continuing the hosted review/fix loop until the remaining acceptance gate closes.
- Refreshed `origin/main` and confirmed it remains at v0.1.1 commit `3ef8844`; the four existing local commits are the Phase 13 design/documentation preparation and design-document split that this implementation builds on.
- Pre-commit review confirmed the current tracked and untracked diff is Phase 13 implementation, tests, release wiring and documentation. Stale current-state wording that said the worktree was uncommitted or push was unauthorized was replaced with revision-based wording; historical vlog entries were retained as records of their original authorization boundary.
- Phase 13 remains `in_progress` until the newly pushed revision's repository CI and six hosted Release Matrix targets provide real success evidence. Exact commit and run identifiers will be recorded after those remote results complete.

## 08:47 — Phase 13 first hosted matrix review and portability fixes

- Committed the reviewed Phase 13 implementation as `bdf5d12` (`feat: implement phase 13 managed semantic vector`) and pushed `main` to `origin`. This triggered repository CI run `35431523497` and Release Matrix run `35431523499`.
- The first hosted runs provided three actionable cross-platform findings. Strict Linux C11 compilation rejected undeclared `clock_gettime`, `CLOCK_MONOTONIC` and `usleep`; macOS Provider artifacts retained an unused Homebrew `libsqlite3.dylib` load command; and the format4 cross-platform fixture still created the pre-Phase-13 four-column metadata table. Repository CI also exposed a scheduler-sensitive Phase 08 retry test whose fixed sleep could release the lock before the actual `SQLITE_BUSY` retry boundary.
- The concurrency probe now opts into POSIX.1-2008 before system headers and uses `nanosleep`. The standalone Provider's macOS link adds `-dead_strip_dylibs`, retaining its host-API-only SQLite boundary even if `libsqlite3-sys` emits an unused `-lsqlite3`. The storage fixture now creates both format4 Semantic cache policy columns. The retry regression now holds the write lock until its connection-level busy callback proves the worker observed `SQLITE_BUSY`, then releases it and verifies recovery.
- Targeted verification passed: the Phase 08 integration suite 27/27, Provider tests 22/22, strict C11 concurrency compile/run, format4 fixture create/reopen, normal Provider artifact inspection, and a forced macOS `-lsqlite3` link simulation whose final dylib still had no SQLite dependency.
- Fresh `scripts/ci.sh` passed across the full Rust suite, real SQLite 3.45.0 and 3.53.4 extension loading, both Phase 13 18-check probes, Provider smokes, concurrency/WAL and performance gates. Fresh `cargo make quality` also passed: coverage regions/functions/lines is **83.44% / 84.24% / 85.23%**, and inherited openCypher TCK remains 3,777/3,777.
- These fixes are locally complete but not yet hosted-verified. Phase 13 remains `in_progress` until the fix revision passes repository CI and all six Linux/macOS/Windows x64/arm64 Release Matrix jobs.

## 09:16 — Phase 13 hosted acceptance closure

- Committed and pushed the first hosted portability fixes as `20304d5` (`fix: close phase 13 hosted portability gaps`). The next repository CI `35433006205` and Release Matrix `35433006171` exposed the same remaining strict-POSIX visibility issue in the separate performance probe; the already-reached macOS and Windows targets showed no independent new behavior finding.
- Revision `672c360` (`fix: expose POSIX timing in phase 13 probe`) added the required POSIX feature-test macro. The exact performance probe and a fresh full `scripts/ci.sh` both passed locally after that change.
- Repository CI `35433504169` then passed quality and Linux/Windows/macOS Phase 01 gates. Release Matrix `35433504174` passed the storage fixture plus Linux x64/arm64, macOS x64/arm64 and Windows x64/arm64 artifact build/load/migration jobs; its publish job was correctly skipped because the revision was not a tag.
- SV13-27 is therefore `done`, and Phase 13 is `done`. No tag, GitHub Release or deployment was performed; v0.1.1 remains the current published baseline.

## 19:22 — Phase 13 ordinary-query persistent Embedding cache

- Corrected the Phase 13 cache contract: normal Semantic search now performs the same persistent read-through flow for query and source text when cache is enabled and `main` is writable. `rebuild` remains optional maintenance instead of a search prerequisite.
- Provider results are fully validated before a short atomic cache publish. Cache-disabled and read-only databases retain the Provider + TEMP/LRU fallback; Provider I/O, cancellation, invalid vectors and fault-injected cache publication do not leave reusable partial rows or move the Branch/create a Commit.
- A real WAL concurrency probe found that a SQL scalar's stale read snapshot cannot upgrade after a concurrent writer commits. The query path now retries only the derived-cache publication through a short sibling connection to the same database file; Provider I/O and graph reads remain on the original pinned Snapshot. With a 1000 ms Provider wait, SQLite 3.45.0 and 3.53.4 both observed writer wait 0 ms, non-autocommit Provider calls 0 and 3 persistent query/source entries.
- Provider counters prove the cold query used 16 inputs for 64 owners/16 unique exact texts, wrote 16 entries/256 bytes, and same-connection plus process-reopen queries used 0 Provider inputs. A later rebuild reported 16 cache hits and 0 Provider inputs.
- `scripts/ci.sh` passed with the expanded 20-check Phase 13 real-extension probe, full workspace regression, SQLite 3.45.0/3.53.4 dual-extension/Native/concurrency/performance gates and the existing compatibility inventory. Final `cargo make quality` also passed with regions/functions/lines coverage **83.42% / 84.22% / 85.22%**, duplicate lines **0.99%**, and inherited openCypher TCK **3,777/3,777**. This worktree remains uncommitted and was not pushed or released.

## 13:05 — Phase 14 SQL Explicit Transaction Adapter 验收闭合

- 先修订 SQL Bridge、Explicit Transaction 与相关接口/存储设计，再新增 Phase 14 计划；四个有副作用 SQL scalar `lithograph_tx_begin/execute/commit/abort` 使用 `SQLITE_DIRECTONLY` 且不声明 deterministic。
- SQL 与现有 C ABI 复用同一个 connection-local transaction state、execution event、Commit finalize 与 fail-closed cleanup；`tx_execute` 把 `COLUMNS -> ROW* -> SUMMARY` 组装为完整 SQL JSON envelope，结果长度/构造失败在可回滚边界内处理。
- 新增真实 C driver smoke，覆盖 multi-write single Commit、staged read、参数绑定、纯读、empty-delta、abort、执行/受限 query/结果失败、outer transaction、duplicate/no-active、expectedHead、参数错误、跨连接可见性与 close cleanup；Python 标准 `sqlite3` 示例可直接运行。
- Review 修复两个 finding：SQL commit/begin panic 必须 fail-closed；重复 begin 的前置拒绝不得错误回滚原有 transaction。Coverage review 又发现新 smoke 未进入 instrumented path，现已接入 `quality-coverage`，`scalar.rs` line coverage 从 48.77% 提升到 82.10%。
- `quality-coverage` exit 0，regions/functions/lines 为 **83.46% / 84.29% / 85.22%**，applicable inherited openCypher TCK **3,777/3,777**；此前 quality 的 format、Clippy、Rustdoc、Lizard、duplication、unused dependency 与 supply-chain gate 均通过。
- 最终 `scripts/ci.sh` exit 0：SQLite 3.51.0、最低 3.45.0 与冻结 3.53.4 均通过 SQL transaction smoke；Native ABI、Phase 01–13、Provider、并发/性能与 compatibility 回归保持通过。Phase 14 标记 `done`。
- Phase 13 / v0.2.0 基线已单独提交为 `232a499`；本次 Phase 14 改动未提交、未推送、未发布。

## 14:15 — v0.2.0 发布与 Phase 14 候选复审

- v0.2.0 文档命令修正提交 `8f91c92` 已推送；`v0.2.0` annotated tag 的 repository CI `35446412962` 与六目标 Release Matrix `35446413043` 全部通过。GitHub Release 已正式发布，包含 Linux/macOS/Windows x64/arm64 六个平台包与 `SHA256SUMS`，共 7 个资产。
- Phase 14 发布前复审补强了 rollback 证据：SQL C smoke 不只检查业务图，还快照并核对 Commit、Layer、Schema object、`main` Branch head 与 `lithograph_integrity_check()`；abort fixture 同时 staged graph 与 RANGE INDEX，覆盖 Schema rollback。
- 同一真实 C driver 已接入 Linux/macOS SQLite 3.45.0、3.51.0、3.53.4 路径，并新增到 Windows x64/arm64 对 SQLite 3.45.0 与 3.53.4 的 artifact smoke；C complexity gate 也纳入该文件。
- Fresh `scripts/ci.sh` 与 repository quality 全部通过；coverage regions/functions/lines 为 **83.44% / 84.29% / 85.22%**，`scalar.rs` line coverage 为 **82.10%**，applicable inherited openCypher TCK 为 **3,777/3,777**。本地没有 PowerShell，因此 Windows SQL smoke 必须由本次 revision 的 hosted Release Matrix 给出最终证据。
- Phase 14 仍保持 `in_progress`，v0.2.1 仍是 release candidate；提交并推送候选 revision 后，只有 repository CI 与六目标 Release Matrix 均通过才关闭 TX14-10 和 Phase 14。

# 2026-09-11

Timezone: UTC

## 00:24 — Phase 03 frontend review closure

Scope and authorization: 按 Phase 03 acceptance 持续 review、修复 correctness/compatibility/quality findings 并重复验证；未授权 commit 或 push。

### Findings and Changes

- 修复 frozen `CY25-2026.08` grammar 偏差：query preamble/options 与 `EXPLAIN` / `PROFILE` 组合、`RETURN/WITH ALL`（含 parenthesized expression）、精确 Match Mode、numeric-start parameter、braced conditional `UNION`、空 conditional branch 拒绝及 query-option Float 词法优先级。
- 补齐 Lithograph-owned AST 中会影响后续执行的 typed discriminator，避免 Phase 04–08 从 raw query text 重建 query options、projection/match/order、mutation、transaction、ingestion、label/type expression 与 schema/index/Graph Type semantics。
- multi-token AST discriminator 改为依据 parser rule tree 分类，comment/newline 不再改变 `UNION ALL`、Match Mode、MERGE action、transaction、constraint 或 SHOW target 的语义分类。
- 修复 braced/conditional query 的 semantic traversal 与 CALL subquery output detection；补充 4 个 frozen Cypher 25 parser fixtures，并让 `phase03_parser_tck` 直接运行 parser-positive fixture inventory。
- 为保持质量门禁，将 parser kind lowering 拆分到 `cypher/parser/kind.rs`，将 AST regression tests 拆分到 `phase03_ast.rs`；production Rust 文件继续低于 650 行，测试文件低于 repository soft file budget。
- 补充 `docs/design.md` 对 Cypher language preamble/options 与 Lithograph execution-level Query Options 两套独立接口的边界说明。

### Verification

- Phase 03 core：`phase03_frontend` 22/22、`phase03_ast` 4/4、`phase03_value_order` 4/4，共 30/30；`phase03_parser_tck` 4/4，继承 corpus 继续保持 4,224 parser / 3,312 frontend-success / 585 compile-error 与精确 16 个 deferred scenarios。
- `cargo make quality` 全绿：jscpd duplicated lines 0.86%；coverage regions 82.50%、functions 84.51%、lines 84.18%；RustSec advisories、licenses、sources、unused dependency、Lizard、Clippy、Rustdoc、file budget 全部通过。
- `scripts/ci.sh` exit 0：SQLite 3.51.0 与最低 3.45.0 的真实 `.load`、Phase 01/02/03 probes、artifact inspection、Native ABI、compatibility harness/inventory 全部通过；harness self-check 的 1 个 intentional failure 为预期自检结果。
- LCE1 frozen golden hash 保持 `cea822c1c96dd7c456beae36aa4e9e89d8d3cad8658aa450b3ede2887ade4440`。

### Remaining

Phase 03 review scope 内当前未发现未闭环 finding；Phase 03 保持 `done`，Phase 04 保持 `ready`。完整 aggregation/order/function/procedure/result semantics 仍由 compatibility matrix 指定的后续 Phase 负责。本次未 commit、未 push。

## 01:28 — Phase 03 second review hardening

Scope and authorization: 再次按 Phase 03 acceptance/code path 持续 review → 复现 → 最小修复 → regression → 再 review；只处理当前 frontend/value contract 内可证明的问题，未扩展 Phase 06+ execution semantics，未授权 commit 或 push。

### Findings and Changes

- 修复 Label/Relationship Type 重复 `!` 在中间存在合法 whitespace/comment 时 AST negation count 丢失的问题；计数改为基于显式 parser operator node，而不是 raw source 连续字符。
- 修复 `CASE` 结果类型推断遗漏 `WHEN ... THEN ...` result branch；AST 增加 `CaseAlternative`，simple `CASE` discriminant 不再被误当作 result type。
- 修复 comparison static typing 只验证右侧 suffix 的不对称问题；左右 operand 现在都会递归完成类型验证。
- 按 vendored openCypher equality scenarios 修正 cross-family Cypher equality：不同 value family 返回 `false`，而不是抛 `ValueError`；numeric cross-type、null three-valued logic 与同-family 特殊语义保持不变。
- 修复公开可构造 malformed `PathValue` 进入 `cypher_order_compare` 时可能数组越界 panic；非法 path shape 现在通过既有 `Result` boundary 返回 `ValueError`。

### Verification

- Phase 03 core 30/30、`phase03_parser_tck` 4/4、extension 7/7、Phase 02 regression 21/21 全部通过；4,224 parser / 3,312 frontend-success / 585 compile-error corpus 与精确 16 个 deferred scenario ownership 未回退。
- `cargo make quality` 全绿：jscpd duplicated lines 0.86%；coverage regions 82.53%、functions 84.53%、lines 84.24%，`value_order.rs` line coverage 88.76%；format、Clippy、Rustdoc、Lizard、file budget、unused dependency、RustSec/license/source policy 全部通过。
- `scripts/ci.sh` exit 0：SQLite 3.51.0 与最低 3.45.0 的真实 `.load`、Phase 01/02/03 probes、artifact inspection、Native ABI、compatibility harness/inventory 全部通过；LCE1 golden hash 保持 `cea822c1c96dd7c456beae36aa4e9e89d8d3cad8658aa450b3ede2887ade4440`。
- 本地缺少 Phase 00 已固定的 Lizard 1.24.0；按既有工程基线安装到用户 Python 环境后继续执行 canonical quality gate，未新增仓库 dependency 或配置。

### Remaining

完成上述修复后的再次静态 review、Phase 03/02 regression、repository quality 与真实 SQLite CI 均未发现新的 task-affecting finding；Phase 03 保持 `done`，Phase 04 保持 `ready`。本次仍未 commit、未 push。

## 07:03 — Phase 04 read-engine review closure

Scope and authorization: 按 Phase 04 acceptance 持续执行 review → 修复 → regression → 再 review，直到 correctness、compatibility、streaming/cancellation、SQLite ABI 与 repository quality finding 闭环；未授权 commit 或 push。

### Findings and Changes

- 修复独立 `MATCH` clause 之间错误共享 relationship uniqueness state 的结果集缺失；同一 `MATCH` 内仍保持 relationship uniqueness。
- 修复多个 expand 的 physical plan relationship type/direction 可能错误复用第一个同起点 pattern spec。
- 修复 projection alias 只支持裸 `ORDER BY alias`、不能参与 `ORDER BY alias + ...` expression 的问题，并在 prepare 阶段拒绝缺失 parameter。
- 按 vendored openCypher NaN evidence 修复 Float 除零语义，`0.0 / 0.0` 与正负 infinity 不再被误报为 division-by-zero error；Integer 除零仍报错。
- 修复 Phase 04 对后续 Phase 才拥有的 syntax 静默近似执行：negated/disjunctive/dynamic label/type expression、inline property/`WHERE`、variable/quantified path、显式 Match Mode/SEARCH、`count(DISTINCT ...)` 等现在明确拒绝，不返回错误语义结果。
- 修复 query SQLite error adapter 把 `BUSY/LOCKED` 与 I/O/read-only 类错误统一压成 `STORAGE_ERROR` 的公共合同偏差。
- 补齐 `docs/design.md §7.5` 已要求的 host `sqlite3_interrupt()` observation：core 接受安全的 interrupt checker，在 direct/aggregate/spill/sort boundary 检查；loadable-extension boundary 从 SQLite append-only API table 取得 3.41+ 的 `sqlite3_is_interrupted`。review 中用 C `offsetof` 验证真实 ABI slot 为 `266`，并在 SQLite 3.51 与最低 3.45 fixture 上通过真实 `.load`/Phase probes。
- Phase 04 regression 增长后把测试拆到 `tests/phase04_query/regressions.rs`，消除 test file soft-budget warning；新增 spill cancellation loop 一度使 jscpd duplicated lines 达 `1.01%`，抽取共享 row helper 后恢复到 `0.94%`。
- 清理 coverage 产生的 `*.profraw` 临时文件，并让该类 LLVM profile artifact 不再污染 repository status。

### Verification

- `cargo test -p lithograph-core --test phase04_query`：26/26；`cargo test -p lithograph-extension --lib`：8/8。
- `cargo make quality` exit 0：duplicated lines `0.94%`；coverage regions `81.84%`、functions `83.70%`、lines `83.56%`；format、Clippy、Rustdoc、Lizard、file budget、unused dependency、RustSec/license/source policy 全部通过。
- `scripts/ci.sh` exit 0：SQLite 3.51.0 与最低 3.45.0 的真实 `.load`、Phase 01/02/03/04 probes、artifact inspection、Native ABI、compatibility harness/inventory 全部通过；harness self-check 的 1 个 intentional failure 为预期自检结果。
- `lithograph-phase04-streaming`：1,000,000 Node checkpoint，10,000-row baseline RSS `80,576,512 B`，1,000,000-row full scan RSS `80,576,512 B`，增长 `0 B`，低于 256 MiB total / 32 MiB growth acceptance。
- `git diff --check` 通过；coverage `*.profraw` 临时产物已清理。

### Remaining

完成上述修复后的再次静态 review、targeted regression、repository quality、canonical CI、SQLite 3.45 minimum ABI 与 1M streaming acceptance 均未发现新的 Phase 04 finding。Phase 04 保持 `done`，Phase 05 保持 `ready`。本次未 commit、未 push。

## 12:02 — Phase 05 versioned-mutation review closure

Scope and authorization: 按 Phase 05 acceptance 连续完成实现、review、修复与再验证，闭合 Graph View write boundary、transaction/Commit atomicity、adapter cancellation 与 compatibility degradation finding；未授权 commit 或 push。

### Findings and Changes

- 建立 mutating query vertical slice：CREATE/INSERT Node/Relationship、SET/REMOVE property/label、DELETE/DETACH DELETE、MERGE foundation 与 multi-row clause barrier 全部进入同一个 version-aware write transaction；成功写入 canonical Layer/Commit 并 CAS 移动目标 Branch。
- DeltaBuilder 按最终 canonical slot 计算 counters；create-then-delete、SET 后恢复原值等净零 query 不再虚报 side effect，同时仍按设计产生 empty-delta Commit。
- Graph View 在每个 mutating clause boundary 按 staged graph state 校验；隐藏 element、不可见 Relationship endpoint、越界 Label mutation、跨 view DETACH 与 direct-reference/MERGE bypass 均原子拒绝。
- QueryCursor、scalar、rows 与 Native adapter 统一 invocation savepoint 生命周期；修复 late projection/scalar length/callback cancel/fault injection 可能在失败前留下 write 的风险，caller-owned transaction/savepoint 不被 extension 越权结束。
- 修复 DELETE 顺序与 target lowering，Relationship 先于 Node 删除；connected Node plain DELETE 返回 constraint error，重复 row target canonicalize；MERGE 独立 pattern 重置 relationship uniqueness，并在写入前拒绝 null pattern property。
- 修复 nested map literal 错误提升内层 entry，以及 map projection/list comprehension 被静默执行为空集合；未由 Phase 05 拥有的 semantics 现在明确拒绝。EXPLAIN 同样执行 lowering validation，不再隐藏 unsupported match mode。
- 最终 review 发现 write `RETURN ... ORDER BY` 错用 comparison predicate 并吞掉不可排序错误；现与 read executor 共用 `cypher_order_compare` 总排序，跨 family ordering 正确，未定义 UUID ordering 会在 Commit 前报错并回滚。
- ZonedDateTime fixed offset 与其余 PropertyValue family 经过 mutation/history round-trip；IANA named-zone 在 Phase 06 timezone/DST 规则落地前明确返回 type error，避免持久化时静默变成 UTC offset。
- review 中按 repository quality gate 抽取 bound-node candidate 与 DELETE pass helper，production complexity 和 duplicated-lines threshold 恢复通过；继续按职责拆分 planner build、stream projection/write lifecycle、canonical delta 与 Phase 05 execution/history regression，本次涉及的 Rust 文件全部低于 1000 行 soft file budget。

### Verification

- Phase 05 targeted mutation suite 43/43；Core Phase 02–04、frontend/TCK、test-support 与 Extension 8/8 regression 全部通过。
- `cargo make quality` exit 0：无 file-budget warning，duplicated lines `0.96%`；coverage regions `82.63%`、functions `84.92%`、lines `84.71%`；format、Clippy、Rustdoc、Lizard、unused dependency、RustSec/license/source policy 全部通过。
- `scripts/ci.sh` exit 0：SQLite 3.51.0 与最低 3.45.0 的真实 `.load`、Phase 01–05 probes、artifact inspection、Native ABI、compatibility harness/inventory 全部通过；harness self-check 的 1 个 intentional failure 为预期自检结果。
- Phase 05 SQL Bridge probe 的 10 项检查覆盖 write summary、三类 invocation rollback、autocommit/outer transaction/savepoint composition、rows read-only、Graph View rollback 与 reopen history；Native smoke 覆盖 ROW/SUMMARY cancel rollback 与 savepoint fault injection。

### Remaining

完成最终静态 review、43 个 targeted regression、repository quality、canonical CI、SQLite 3.45 minimum ABI 与真实 extension/Native 验收后，未发现新的 Phase 05 scope finding。Mutation capability family 仍按 frozen Profile 规则标为 `partial`；完整 query/path/function/mutation semantics、IANA timezone/DST 与 FOREACH/map/correlated MERGE edge cases 由 Phase 06 接管。Phase 05 转 `done`，Phase 06 转 `ready`。本次未 commit、未 push。

## 23:02 — Phase 05 second review hardening

Scope and authorization: 继续按 Phase 05 design/acceptance 执行静态 review -> 真实复现 -> 最小修复 -> regression -> 再 review，处理已实现 vertical slice 中会返回错误结果、绕过 validation、提前提交或漏看 interrupt 的问题；不提前实现 Phase 06 capability，未授权 commit 或 push。

### Findings and Changes

- write `RETURN DISTINCT` 改用 canonical grouping key 并移到 ORDER BY 前；null、嵌套值和跨 family total ordering 与 read result pipeline 保持一致。
- 修复 expression AST lowering 丢失或重复 descendant 的问题：nested `NOT`、chained comparison、parenthesized postfix property、function argument/`count(expr)` 现在保留实际语义；subscript/slice/label/type postfix 在未实现前明确拒绝。
- CREATE 同 clause 新变量不能被 property expression 提前引用，但已声明 Node 可在同一 pattern 中 bare reuse；MERGE candidate 尊重 bound endpoint，large `ON MATCH` apply loop 持续观察 host interrupt。
- DELETE 对整条 clause 的全部 row 先删除 Relationship 再删除 Node；QueryCursor 未消费完 rows 时不能 `complete()` 提交 invocation savepoint。
- mutating EXPLAIN 现在包含实际 scan/filter/optional/projection/write operators，执行相同 lowering validation，anonymous placeholder 不跨 pattern 形成 binding。
- dynamic/negative/disjunctive write name、inline pattern WHERE、path selector/mode/quantifier、computed SET target 等未拥有语义全部 fail closed，不再被降级为 literal/base expression 后执行并提交。
- aggregate write projection 在 SKIP/LIMIT 前验证 ORDER BY；projected count、alias 与 constant expression 正常执行，未投影 input variable 与不支持的 function 在 Commit 前拒绝并回滚。
- function support 在 lowering 阶段按当前 executor 能力 fail closed，即使 input 为零行也不会跳过 validation；已支持的 `elementId` / `labels` / `type` / `size` 保留实际参数并按 Cypher 语义传播 null。

### Verification

- Phase 05 targeted mutation suite 从 43 增至 67，67/67 通过；新增 24 个 regression 逐项锁定上述 finding。
- `cargo make quality` exit 0：duplicated lines `0.98%`；coverage regions `83.25%`、functions `85.12%`、lines `85.13%`；fmt、全 workspace Clippy/Rustdoc、复杂度、unused dependency、RustSec/license/source policy 全部通过。
- `scripts/ci.sh` exit 0：SQLite 3.51.0 与最低 3.45.0 的真实 `.load`、Phase 01–05 probes、artifact inspection、Native ABI 与 compatibility harness/inventory 全部通过。
- 使用刚构建的 extension 在 SQLite 3.45.0 额外复核：DISTINCT null 为 1 row、nested `NOT` 为 true、`1 < 4 < 3` 为 false、nested parameter property 为 7；`elementId` / `labels` / `type` / nested `size` 返回正确非 null 值，四个 function 对 null 均返回 null；unsupported postfix、同 clause forward reference、aggregate ORDER BY hidden input 与 zero-row unsupported function 均返回 semantic error，随后 label count 为 0，证明 invocation 已回滚。

### Remaining

完成第二轮静态与真实运行 review 后，当前没有可复现的 Phase 05 scope finding。完整 function/aggregation/query/path/mutation inventory、IANA timezone/DST、FOREACH 与 correlated MERGE edge cases 仍由 Phase 06 接管，不能把本轮 fail-closed 证据误读为这些 family 已完成。Phase 05 保持 `done`，Phase 06 保持 `ready`。本次未 commit、未 push。

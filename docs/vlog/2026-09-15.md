# 2026-09-15

Timezone: UTC

## 01:59 — Phase 10 release hardening and scale-index optimization

Scope and authorization: Continue Phase 10 implementation/review/optimization; commit and push the resulting repository changes.

### Decisions

- Keep Phase 10 `in_progress`: the repository-wide quality gate and targeted release-scale regressions pass, but the 10M/100M workload still needs to be rerun after the latest standard-index cache optimization before release acceptance can be closed.
- The 10M Range Index build exposed a concrete performance bottleneck in the derived TEMP cache path. The fix keeps canonical history unchanged and optimizes only rebuildable index cache construction by deferring secondary-index creation and reusing prepared statements/property reads.

### Changes

- Hardened Phase 10 query/index paths, release smoke scripts, README quickstart, and dependency lock state.
- Optimized standard-index cache bulk construction and added bounded checkpoint property-page reuse for large label/property index builds.
- Made the Phase 10 scale runner resumable from the canonical checkpoint and schema setup idempotent for an existing large fixture.

### Verification

Source state and environment: `main` worktree based on `origin/main` at `552e8f6`; macOS arm64, Rust 1.98.1.
Commands, scope, and observed results: `cargo make quality` passed before the final scale-index optimization; final optimization then passed workspace `cargo check`, `cargo clippy -p lithograph-core --all-targets --all-features`, Phase 07 schema/index tests 38/38, production complexity gate, duplication gate at 1.00%, `git diff --check`, and a fresh 10k Node / 100k Relationship release-scale run including Range Index build and all configured workloads.

### Remaining

- Rerun the full 10M Node / 100M Relationship release workload with the optimized Range Index build.
- Execute and verify the six-target hosted release artifact matrix before marking Phase 10 complete.

## 08:28 — Phase 10 local release closure and final review

Scope and authorization: Continue Phase 10 implementation, verification and review to its acceptance boundary. Repository code/docs/test changes are authorized; commit and push are not part of this request.

### Decisions

- Keep Phase 10 `in_progress`: all current-worktree local release gates are closed, but the six-target hosted artifact matrix still lacks a successful run. The latest hosted run at `643ee8d` generated the interop fixture successfully, then failed while finalizing `actions/upload-artifact@v4` with HTTP 403, so target artifact jobs were skipped.
- Preserve canonical storage/version semantics while closing scale blockers: optimize only proven redundant work in Snapshot access, derived standard-index cache construction, first-parent Diff, empty-Commit validation and mutation schema validation. Affected Graph Type/Constraint domains still use the original complete canonical validators.

### Changes

- Reused known Label-scan membership/existence in Graph View execution and kept fallback visibility checks for multi-label/exclusion cases.
- Added set-based checkpoint property-page reads and per-index-kind TEMP cache indexes for standard Index rebuilds.
- Added first-parent touched-Layer Diff, empty-Layer Commit fast path, and Layer-impact-aware mutation schema validation to remove unintended large-graph scans without changing Commit/Branch/Constraint semantics.
- Made the Phase 10 scale runner resumable after interrupted Tag/version workloads and separated pure label streaming from per-row property reads.

### Verification

- Full 10M Node / 100M Relationship workload passed against the existing ~26GB canonical fixture: label scan, equality/range seek, low/high-degree traversal, variable path, full-text, vector SEARCH, historical read, Tag, Commit Data, branch Diff, 4x64 cursor History, and 1,000-Node write/single-Commit all completed correctly with no OOM. The generated baseline is `target/phase10-scale-release/baseline.json` and is intentionally not tracked.
- Final-worktree 10,000-conflict Merge Session passed 40 conflict pages and 40 resolution rounds, candidate inspection, one finalize Commit and Tag GC-root preservation. Native scale smoke on the 26GB database passed two staged writes + staged read -> exactly one final Commit.
- `phase10_hardening` 6/6, `phase10_compatibility` 4/4, `phase10_recovery` 10/10, Phase 05 mutation 67/67, Phase 07 schema/index 38/38 and Phase 09 version 46/46 passed during the repair loop.
- `cargo make quality` passed format, file budget, all-target/all-feature Clippy, rustdoc, complexity, dependency audit, duplication, full tests, coverage and 3,777/3,777 applicable inherited openCypher TCK. `scripts/ci.sh` passed real SQLite load/probes including SQLite 3.45.0 minimum. The freshly rebuilt macOS arm64 release artifact then passed release smoke on SQLite 3.45.0, 3.51.0 and 3.53.4, Native ABI, Phase 01–09 probes and storage fixture interoperability.
- Final architecture/security diff review found no task-affecting finding: no SQLite fork/private API, new dependency, Extension/ABI surface change, second mutable truth or security-boundary change; derived cache remains rebuildable TEMP state and Graph View remains enforced on Snapshot access paths.

### Remaining

- Run and pass the six-target hosted Release Matrix for Linux x64/arm64, macOS x64/arm64 and Windows x64/arm64 on the current code before Phase 10 can be marked `done` or a complete Release can be claimed.

## 08:55 — Phase 10 hosted matrix gate hardening

Scope and authorization: Continue Phase 10 implementation/review/optimization to its acceptance boundary. Repository workflow/docs changes are authorized; commit and push remain separate actions and were not requested in this turn.

### Decisions

- Keep Phase 10 `in_progress` until the six hosted target jobs actually pass on the current code.
- Do not make release correctness depend on GitHub Actions artifact-storage availability. The common interoperability fixture must still be created once on Linux and consumed byte-for-byte by every target runner; release artifact upload remains useful for debugging/downloads but is not itself a correctness gate.

### Changes

- Reworked `.github/workflows/release-matrix.yml` so `storage-fixture` transports the Linux-generated SQLite fixture as a gzip+base64 job output instead of requiring `upload-artifact`/`download-artifact` for the matrix dependency.
- Kept fixture and release-artifact uploads as `continue-on-error` best-effort steps, so a GitHub artifact-storage 403 cannot skip Linux/macOS/Windows build/load/ABI/interoperability acceptance.

### Verification

- Workflow YAML parses successfully with the system Ruby YAML parser; `git diff --check` passes.
- The 148 KiB interoperability fixture compresses to a 4,872-character base64 payload, well below job-output and Windows environment-size constraints used by the workflow.
- Local encode/decode round-trip reproduced the original SQLite fixture byte-for-byte.

### Remaining

- The current worktree still requires commit/push (separate authorization) before GitHub can execute this revised workflow, followed by a successful six-target hosted Release Matrix run. Those remote results are the only remaining Phase 10 acceptance evidence.

## 09:16 — Phase 10 Windows `LOAD CSV` hosted finding

Scope and authorization: Follow the pushed Phase 10 Release Matrix to completion, fix real hosted findings, and commit/push the resulting repair under the user's explicit commit/push authorization.

### Finding

- Release Matrix run `34950029253` successfully passed the shared storage-fixture job and reached all six target jobs. Linux x64, Linux arm64 and macOS arm64 passed their real release gates before this repair; both Windows x64 and Windows arm64 failed in the Phase 08 probe on SQLite 3.45.0 with `LITHOGRAPH_INVALID_ARGUMENT: LOAD CSV file:// supports only local files`.
- The failure was not a DLL load, ABI, build or fixture-transport failure. The Phase 08 probe constructed a Windows path as `file://D:\...`, which is interpreted as a non-local URL host, while the engine also lacked normalization from the standard local Windows form `file:///D:/...` to a drive path.

### Changes

- `LOAD CSV` now normalizes a standard percent-decoded Windows drive file URL path such as `/D:/work/file.csv` to `D:/work/file.csv` only on Windows; Unix behavior and rejection of non-local `file://host/...` remain unchanged.
- Phase 08 release and integration fixtures now generate standard local file URLs with forward slashes, including `file:///D:/...` on Windows.

### Verification

- Windows drive-path normalization unit test passed 1/1.
- Phase 08 search/ingestion integration suite passed 27/27; Phase 08 release probe builds successfully; all-target/all-feature core Clippy and `git diff --check` passed.
- `cargo check` for both `lithograph-core` and the Phase 08 release probe passed under the installed `x86_64-pc-windows-gnu` target, proving the Windows-specific branch type-checks before the hosted rerun.

### Remaining

- Commit/push this Windows file-URL repair and require a fresh six-target hosted Release Matrix before closing Phase 10.

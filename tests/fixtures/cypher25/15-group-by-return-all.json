{
  "schema_version": 1,
  "id": "cy25-group-by-return-all-parser",
  "profile": {
    "id": "CY25-2026.08",
    "source": "https://neo4j.com/docs/cypher-manual/25/clauses/group-by/",
    "version": "2026.07",
    "family": "GROUP BY / RETURN ALL"
  },
  "execution": "enabled",
  "query": "MATCH (n:Item) RETURN ALL n.kind AS kind, avg(n.score) AS score GROUP BY ALL",
  "params": {},
  "initial_graph": [
    "CREATE (:Item {kind:'a', score:1}), (:Item {kind:'a', score:3}), (:Item {kind:'b', score:2})"
  ],
  "expected": {
    "kind": "rows",
    "columns": ["kind", "score"],
    "ordered": false,
    "rows": [
      [
        {"type": "string", "value": "a"},
        {"type": "float", "value": "2"}
      ],
      [
        {"type": "string", "value": "b"},
        {"type": "float", "value": "2"}
      ]
    ]
  }
}

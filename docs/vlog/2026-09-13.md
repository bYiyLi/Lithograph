# 2026-09-13

Timezone: UTC

## 05:09 — Phase 08 Search and Data Ingestion implementation/review closure

Scope and authorization: 用户要求按 Lithograph 当前 Design、Phase 08 plan 与 repository quality/compatibility 规范完整开发 Phase 08，并持续执行 review → 修复 → re-review 直到当前 Phase scope 无剩余 finding；授权 Phase 08 代码、测试、fixture、CI 与 development 文档修改，未授权 commit 或 push。

### Decisions

- 保持 Full-text/Vector physical content 为 `(Commit, Index definition)` keyed derived cache；canonical history 只保存 versioned Index definition。FTS5/HNSW cache 删除后必须 rebuild 或 exact fallback，不允许成为 correctness source。
- `SEARCH` 先应用目标 Snapshot 与 Graph View visibility，再应用 filter/top-k/`LIMIT`；Node/Relationship、historical Snapshot 与 HNSW/exact fallback 共用同一可见性边界。
- `LOAD CSV` 对 `file://` 使用 canonical absolute local path；HTTP/HTTPS 按流式读取同步写入临时本地文件，使 `file()` 返回本地 path 而不整文件 materialize 到内存。`file()` / `linenumber()` 在 LOAD CSV 外返回 `null`，WITH HEADERS 的 header 占 line 1。
- `CALL { ... } IN TRANSACTIONS` 由 Native autocommit execution 拥有 batch transaction boundary；每个成功 mutating batch 一个 Commit、read-only batch 不新增 Commit。SQL Bridge 与 `lithograph_rows` 在 filesystem/network/transaction side effect 前 fail-closed；Native explicit multi-execution transaction 仍属于 Phase 09。

### Changes

- 完成 Full-text DDL/query：Node/Relationship、多 label/type、多 property、analyzer/config、score/skip/limit、Graph View-before-pagination、phrase/boolean/property query syntax、derived FTS5 rebuild 与 historical definition/cache isolation。
- 完成 Vector/HNSW 与 `SEARCH`：versioned Vector config、dimension/similarity/quantization/HNSW metadata、additional filter properties、Node/Relationship binding、OPTIONAL/null query、`SCORE`/`LIMIT`、hidden-candidate top-k、planner operator、historical Snapshot、HNSW cache 与 exact fallback。
- 完成 `LOAD CSV`：file/HTTP/HTTPS、headers/no headers、multiline quoted fields、double-quote/backslash quote escaping、custom/Unicode `FIELDTERMINATOR`、`file()` / `linenumber()`、streaming mutation、malformed/I/O rollback 与 IN TRANSACTIONS streaming batching。修复了 parser 已接受 FIELDTERMINATOR 但 runtime 仍使用逗号的执行缺口，并补齐 Cypher string `\uXXXX` 解码。
- 完成 transaction batching：batch size、FAIL/CONTINUE/BREAK/RETRY/status、DISJOINT BY、concurrent syntax、per-batch latest-head repin、Graph View recomputation、Commit cardinality 与 transaction boundary errors；Native ABI smoke 增加真实 batching/caller-transaction negative path。
- 新增 Phase 08 real-extension probe 并接入 `scripts/ci.sh`；补齐 SQL Bridge `TRANSACTION_BOUNDARY_REQUIRED`、rows `READ_ONLY_ADAPTER` 与 scalar LOAD CSV 集成证据。
- 最终交付 review 发现最低 SQLite 3.45 smoke 仍只执行到 Phase 07；将 Phase 08 probe 接入 `scripts/sqlite-345-smoke.sh` 后重新执行 canonical CI，确认 3.51.0 与 3.45.0 均真实覆盖 Phase 01–08。
- review loop 修复 registry inventory、Phase 06 stale regression、Vector SHOW numeric option type、full-text flat Lucene `+/-` translation、SEARCH null/pattern/filter edge、file budgets/complexity/duplication 与 HTTPS dependency license allowlist；jscpd 从本轮中途 1.66% 收敛至 0.99%，未放宽任何 quality threshold。
- 同步 README、Phase 08/09 状态与 Cypher 25 compatibility matrix：Phase 08 `done`，Phase 09 `ready`。

### Verification

Source state and environment: `main...origin/main`；本任务全部修改仍在工作树，未 commit、未 push。真实 Extension gate 使用 SQLite 3.51.0，并通过最低支持 SQLite 3.45.0 smoke。
Commands, scope, and observed results: Phase 08 targeted suite 23/23；enabled CY25 execution fixture regression 通过；`cargo make quality` exit 0，duplicated lines 0.99%，coverage regions/functions/lines 81.63% / 84.49% / 83.71%，format、strict Clippy、Rustdoc、Lizard、hard file budget、cargo-machete、cargo-deny 全部通过。最终 `scripts/ci.sh` exit 0：SQLite 3.51.0 / 3.45.0 real `.load`、Phase 01–08 probes、extension crate tests、artifact inspection、Native ABI smoke、compatibility harness/inventory 与 openCypher TCK inventory 全部通过；harness self-check 的 1 个 intentional failure 为预期自检结果。

### Remaining

当前 reviewed Phase 08 correctness、Cypher 25 Search/Ingestion contract、Graph View isolation、historical derived-cache boundary、transaction durability/error semantics、external-I/O safety 与 repository quality gate 范围内未发现剩余 task-affecting finding。10M/100M scale、cross-platform release、最终 PROFILE/error compatibility closure 仍按既有 owner 留在 Phase 10；Version operations / Native explicit transaction 由 Phase 09 接管。本次未 commit、未 push。

## 10:20 — Phase 08 post-completion review hardening

Scope and authorization: 用户继续要求对已完成 Phase 08 按仓库规范执行 review → 修复 → re-review，直到当前 scope 无剩余 finding；本轮未授权 commit 或 push。

### Findings and changes

- 复现并修复普通 `LOAD CSV` 在 `CREATE -> MATCH` 等多 clause mutation 中按 CSV record 重跑整个 tail、破坏 Phase 05 clause barrier 的 correctness bug；同一修复覆盖 read-side global `ORDER BY`/aggregation 与超过单 spill batch 的全局语义。
- spill binding round-trip 补齐 Node/Relationship/Path identity，锁定 `LOAD CSV -> MATCH -> SET/REMOVE/DELETE/MERGE`；HTTP/HTTPS source 改为 reader 读完即关闭，仅保留 `TempPath` guard，避免大量 correlated source 同时持有 response/file handle。
- `file()` / `linenumber()` context 可穿过保持同一 CSV row context 的 projection；`LOAD CSV ... WITH count(...) ... IN TRANSACTIONS` 在 batching 前先完成 global prefix，避免每 CSV row 各自产生 `count=1`。
- 新增回归使 Phase 08 test file 一度超过 1400 hard budget，并使新增 spill path 触发 complexity/duplicate gate；按测试职责拆分回归，并只抽取已被重复证明需要的 Binding spill batch、CSV source streaming 与 stateful write spill helper，最终 duplicated lines 收敛到 0.99%。
- review 发现 quality coverage 使用固定 `target/llvm-cov-integration`，并行 `cargo make quality` 会互相 `rm -rf` 构建目录；改为每次使用独立 `mktemp` target 并由 shell trap 清理。并发 quality 存在时重新运行 coverage 仍成功，证明门禁竞态闭合。
- compatibility matrix 的 Phase 08 targeted evidence 从历史 23 scenarios 同步为当前 27 scenarios，并补记 clause barrier、CSV context、graph-element spill 与 global-prefix batching regression。

### Verification

- `phase05_mutation` 67/67、`phase06_query` 54/54、`phase08_search_ingestion` 27/27；真实 SQLite Bridge 手工对照确认 `LOAD CSV CREATE -> MATCH` 与等价 `UNWIND CREATE -> MATCH` 都观察完整 clause barrier。
- final `cargo make quality` **exit 0**：jscpd duplicated lines **0.99%**；coverage regions/functions/lines **81.78% / 84.38% / 83.93%**；format、strict Clippy、Rustdoc、Lizard、hard file budget、cargo-machete、cargo-deny 与 Phase 01–08 real-extension coverage probes 全部通过。
- final `scripts/ci.sh` **exit 0**：SQLite 3.51.0 / 3.45.0 real `.load`、Phase 01–08 probes、extension tests、artifact inspection、Native ABI smoke、compatibility harness/inventory 与 openCypher TCK inventory 全部通过；harness self-check 中的 intentional failure 仍是预期自检。

### Remaining

当前 reviewed Phase 08 correctness、Search/Ingestion observable semantics、Graph View/historical isolation、transaction durability/error contract、external-I/O resource safety 与 repository quality/CI 范围内无剩余 task-affecting finding。Phase 10 的 release-scale/cross-platform/final compatibility owner 与 Phase 09 的 Version/Native explicit transaction owner 未改变。本轮未 commit、未 push。

## 11:03 — Phase 08 LOAD CSV context final review closure

Scope and authorization: 用户继续要求 Phase 08 review → 修复 → re-review 直到当前 Phase scope 无剩余 finding；本轮仍未授权 commit 或 push。

### Findings and changes

- 复现并修复 scoped `CALL` import 丢失 `LOAD CSV` execution context，导致子查询内 `file()` / `linenumber()` 返回 `null` 的 correctness bug。
- 复现并消除内部 CSV context 与合法 Cypher 用户变量的命名空间碰撞：不再借用 `BindingRow.values` 中的 `__lithograph_load_csv_*` 字符串 key，而是使用独立内部 `LoadCsvContext`；`RETURN *` 与公开 column schema 不暴露该状态。
- context spill/projection/aggregation 保留原有语义：file 与 line 可独立保留/失效；Binding spill 序列化显式保存内部 context。
- 复现并修复 nested read/mutating `CALL` 中更晚执行的第二个 `LOAD CSV` context 在返回 outer pipeline 时丢失的问题；CALL merge 现在仅在 inner 确实有更晚 context 时覆盖 outer，使多个/嵌套 `LOAD CSV` 的 `file()` / `linenumber()` 指向最近实际执行 clause。
- 最终文档证据明确区分真实 execution report 与 metadata inventory：18 个 frozen fixtures 全部 `execution: enabled`，`phase06_compatibility` 真实 core executor 为 18 passed / 0 failed / 0 planned；`lithograph-compat inventory` 不执行 fixture，因此 standalone inventory report 按设计显示 planned。

### Verification

- `phase05_mutation` 67/67、`phase06_query` 54/54、`phase08_search_ingestion` 27/27。
- final `cargo make quality` **exit 0**：jscpd duplicated lines **0.99%**；coverage regions/functions/lines **81.78% / 84.35% / 83.94%**；format、strict Clippy、Rustdoc、complexity、hard file budget、unused dependency、advisory/license/source policy、Phase 01–08 real-extension coverage probes 全部通过。
- final `scripts/ci.sh` **exit 0**：SQLite 3.51.0 与最低支持 3.45.0 real `.load`、Phase 01–08 probes、workspace/extension tests、artifact inspection、Native ABI smoke、compatibility self-check/inventory、真实 enabled fixture execution regression 与 openCypher TCK inventory 全部通过；self-check 的 intentional failure 为 harness 预期行为。

### Remaining

本轮最终只读审计未发现新的 Phase 08 task-affecting finding；Search/Ingestion、LOAD CSV context、Graph View/historical isolation、transaction durability/error contract、external-I/O safety 与 repository quality/CI 在当前 Phase acceptance 内闭环。Phase 09/10 的既有 owner 不变。本轮未 commit、未 push。

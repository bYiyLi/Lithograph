# 2026-09-10

Timezone: UTC

## 12:57 — 收敛 Graph View 执行合同

Scope and authorization: 根据 Graph View 设计 review findings 优化 Lithograph 设计真源与受影响的后续 Phase 计划；不修改实现、不提交、不推送。

### Decisions

- `graphView` selector 在一次 execution 内固定，但 element visibility 随 Cypher 当前 clause graph state 计算；依据 `docs/design.md` §7.4/§7.6 与 Cypher 25 clause-composition 语义。
- Graph View 不投影 Schema / Constraint / Index；完整 versioned Schema 对完整 mutation candidate graph 保持有效，hidden element 可触发正常 `CONSTRAINT_ERROR`；依据 `docs/design.md` §7.6。
- selector 输入边界固定为 `requireAllLabels` / `excludeAnyLabels` string set；unknown Label resolution 不创建 dictionary entry；依据 `docs/design.md` §4.4。
- `CALL { ... } IN TRANSACTIONS` 的每个 batch 使用同一 selector，但针对各自 pinned Snapshot 重新计算 visibility；依据 `docs/design.md` §7.4/§9.5。

### Changes

- 更新 `docs/design.md` 的 Query Options、Snapshot Pinning、Graph View write/read/Schema boundary、transaction batching 与 D9。
- 同步 Phase 04–08、Phase 10 acceptance/review，并补充 `docs/research/reference-baseline.md` 的 Cypher 25 clause-composition / transaction-batching 证据。

### Verification

Source state and environment: Yi Mac `/Users/yi/Code/Lithograph` 当前未提交 worktree；Phase 03 保持既有文档内容，没有引入 Graph View 变更。
Commands, scope, and observed results: Graph View contract consistency checks passed；15 个 design/development 文档 local links passed；`git diff --check` passed；Phase 03 Graph View leakage check returned none。

### Remaining

本次 review 的 4 个 Graph View 合同 finding 已在文档范围闭环。Graph View 首次实现仍由 Phase 04 负责；当前仓库其它未提交 Phase 02/03 开发改动保持原状。

## 15:51 — Phase 03 前端与值语义收口

Scope and authorization: 按 Phase 03 acceptance 持续 review、修复 correctness/compatibility/quality findings，直到 repository gate 闭环；未授权 commit 或 push。

### Changes

- semantic legality 从 raw query text heuristic 收敛到 Lithograph-owned AST：覆盖 CALL import、LOAD CSV binding、relationship direction、YIELD alias、escaped identifier、grouping reference、expression-subquery kind 与 unary INTEGER64 min boundary；interpolation error span 映射回原 query。
- 补齐 Type/Vector/UUID/Temporal/conversion function compile-time validation，并把 direct comparison 与 `ORDER BY` total ordering 分离；Duration tagged JSON 输出改为 component-driven canonical text。
- Lithograph JSON 对 tagged value shape、Temporal、Point、Vector、UUID 与 persistent-property boundary 做 exact validation；Phase 03 production Rust 按现有职责拆分并保持单文件不超过 650 行。
- inherited openCypher deferred compile-error boundary 从 40 收缩到精确 16 个 scenario：7 个 procedure catalog/signature、1 个完整 built-in function inventory、8 个 aggregation/DISTINCT `ORDER BY` visibility/grouping；测试锁定精确集合，不能等量替换。

### Verification

Source state and environment: Yi Mac `/Users/yi/Code/Lithograph`，当前 worktree 仍包含既有未提交 Phase 02/03 与其它用户改动；本次未 reset、未 commit、未 push。
Commands, scope, and observed results: Phase 03 core integration tests 25/25（frontend 21 + value-order 4）；`phase03_parser_tck` 3/3；Phase 02 storage 13/13、checkpoint integrity 8/8、extension 7/7 回归通过；`cargo make quality` 全绿，jscpd duplicated lines 0.88%，coverage regions 82.21% / functions 83.95% / lines 83.84%；`scripts/ci.sh` 在 SQLite 3.51.0 与最低 3.45.0 上完成真实 `.load`、Phase 01/02/03 probes、artifact inspection、Native ABI 与 compatibility inventory。

### Remaining

Phase 03 scope 内本轮 review findings 已闭环，保持 `done`；Phase 04 可按 `ready` 进入开发。上述 16 个 deferred compile-error scenario 由 compatibility matrix 指定的后续 Phase 继续负责，不属于 Phase 03 未完成项。

# 2026-09-17

Timezone: UTC

## 03:51 — Phase 12 Full-text / FTS5 Tokenizer implementation closure

Scope and authorization: 用户持续要求按照 Lithograph 当前 Design、Phase 12 计划与仓库规范完成 Full-text / FTS5 Tokenizer 扩展，并持续执行 review → 修复 → re-review，直到本次范围内没有剩余 finding；授权代码、测试、CI/quality wiring 与文档修改，未授权 commit、push、发布或部署。

### Decisions

- Full-text analyzer 直接保存并委托宿主 SQLite connection 已注册的 FTS5 tokenizer specification；Lithograph 不维护 tokenizer 名称白名单，也不把 `english` / `standard-no-stop-words` 映射为内部实现。原生 FTS5 unsafe/FFI 仅隔离在 `lithograph-fts5`，Core 保持 `forbid(unsafe_code)`。
- Query-time analyzer 使用 scoped query tokenizer child，按 LIFO 恢复并 exactly-once 释放；完整 MATCH 表达式继续由 FTS5 解析，不再把 query 近似成去重词集合。额外 TEMP corpus 按 Snapshot/IndexDefinition 有界复用，不按 analyzer/query string 增长。
- Schema CREATE、Patch、Merge 等真正发布新 Full-text definition 的入口在 canonical Schema 持久化前执行原生 tokenizer constructor 验证；read-only planning、SHOW、DROP 和 `IF NOT EXISTS` 已存在定义不触发无关构造。
- 真实 FTS5 tokenizer `xCreate` 返回 `SQLITE_NOMEM` 时，SQLite scalar callback 内会保持 malloc-failed 状态，导致 invocation SAVEPOINT cleanup 也返回 NOMEM。SQL Bridge 保留真实 primary code、禁止发布未验证 Schema，并把连接视为 cleanup-failure quarantine；调用方必须关闭丢弃。Native API 继续按自身 transaction boundary fail closed。
- 最低/冻结 SQLite 的 Native smoke 直接使用各自 amalgamation source 编译 C harness，避免自建 sqlite executable 错配系统 header/lib。

### Changes

- 新增 `crates/lithograph-fts5`、Phase 12 Core targeted suite、真实双 extension `lithograph-phase12` probe、synthetic tokenizer C extension 与构建脚本。
- 更新 Full-text configuration、cache/history、query-time analyzer、Schema/Version publication validation，以及 SQL Bridge/Native staged transaction failure paths。
- 将 Phase 12 real-extension/Native smoke 接入 `scripts/ci.sh`、SQLite 3.45.0/3.53.4 smoke，并修复 Native harness 的 frozen-runtime header/lib 绑定。
- 多轮 review 修复 SQL NOMEM cleanup oracle、SQLite 3.53.4 canonical error text、merge hard file budget、test-support Lizard CCN、Native C function-length gate和 Clippy finding；未放宽任何 quality threshold。
- 同步 Design、CHANGELOG、Search guide/reference、Phase 12 acceptance、Development roadmap 与 Cypher compatibility supplemental inventory。

### Verification

Source state and environment: HEAD `463fb7c5f372c097d7dae776d271936f8b25b68a` (`main == origin/main`) + 当前未提交 Phase 12 worktree；macOS arm64；host SQLite 3.51.0，最低支持 SQLite 3.45.0，冻结 runtime 3.53.4。未执行 commit、push、发布或 hosted Release Matrix。

Commands, scope, and observed results:

- `cargo test --locked -p lithograph-fts5`：4/4 passed；`cargo test --locked -p lithograph-core --test phase12_fulltext_tokenizer`：11/11 passed。
- executable inherited openCypher TCK：3,897 total，3,777 applicable 全部 passed，0 failed；120 个 non-applicable scenario 全部保留 machine-readable exclusion reason。
- `cargo make quality`：exit 0；production duplicated lines 0.99%；coverage regions/functions/lines 83.26% / 84.35% / 85.26%；file budget、fmt、Clippy、Rustdoc、Lizard、machete、deny 均通过。
- `scripts/ci.sh`：最终单次执行 exit 0；host 3.51.0 与 frozen SQLite 3.45.0/3.53.4 real `.load` probes 全部通过；两套 frozen runtime 都实际执行 Phase 12 synthetic tokenizer 与 Native ABI smoke；artifact inspection、compat self-check/inventory/tck-inventory 均完成。
- Phase 12 integration probe 覆盖 direct FTS5 oracle、两种 extension load order、constructor/tokenization failure、connection-local registration、Merge publication validation；Native smoke覆盖 staged visibility/commit、普通 constructor failure、`SQLITE_NOMEM` resource failure 与 fail-closed transaction cleanup。

### Remaining

当前 reviewed Phase 12 scope 内没有剩余 task-affecting finding；FT12-01–20 与本地 repository acceptance 已闭合。代码和文档仍是未提交 worktree；commit、push、新版本发布和 hosted six-platform Release Matrix 均未执行，也未作为本次完成声明的一部分。

## 04:44 — Phase 12 continuation review closure

Scope and authorization: 继续按当前仓库规范复核 Phase 12 的实现、验收状态与最终 diff；允许直接修复本次 review finding 并重新验证，仍未授权 commit、push、发布或部署。

### Decisions

- 本轮发现的有效 finding 是状态文档残留，不需要重新设计 Full-text 合同：`docs/design.md` 顶部、根 `README.md` 与 `docs/development/architecture-roadmap.md` 仍把 Phase 12 写成 `ready` / 尚未实现，与同一 worktree 已闭合的 FT12-01–20 相冲突。
- 统一改为“Phase 00–12 已完成开发验收”，同时继续明确区分当前开发版本与已发布 v0.1.0；没有把未执行的新版本发布或 hosted Release Matrix 写成已完成。
- 重新 review FTS5 specification parser、dual-tokenizer adapter 以及 Schema/Patch/Merge/Rebase 等 publication validation call sites。用真实 SQLite 对空格、tab/newline、单引号/双引号 tokenizer directive 做差分探针，当前 parser 对本次支持语法的接受/拒绝行为与宿主一致，因此没有代码 finding。

### Changes

- 修正 `docs/design.md` 顶部 Phase 状态与 v0.1.0 发布边界说明。
- 修正根 `README.md` 的当前开发状态与 Phase 12 发布状态。
- 修正 `docs/development/architecture-roadmap.md` 的 Critical Path 和 Phase 12 状态说明。

### Verification

- `cargo test --locked -p lithograph-fts5`：4/4 passed。
- `cargo test --locked -p lithograph-core --test phase12_fulltext_tokenizer`：11/11 passed。
- `cargo make quality`：exit 0；coverage regions/functions/lines 83.26% / 84.35% / 85.26%；production duplicated lines 0.99%；fmt、Clippy、Rustdoc、Lizard、machete、deny 与 coverage gate 均通过。
- 真实 SQLite tokenizer directive probe：空格分隔与单引号参数按预期接受；tab/newline/CR 分隔及双引号/backtick/bracket 参数按宿主语法拒绝，和 adapter parser 行为一致。
- 当前变更 Markdown：11 个文件、92 个相对链接全部存在；7 个未跟踪文件 trailing-whitespace scan 通过；`git diff --check` 通过。
- stale Phase 12 status scan 与 production legacy analyzer mapping scan 均无命中；最终 review 未发现新的 task-affecting finding。

### Remaining

Phase 12 当前本地开发与验收状态保持 `done`。本轮没有修改已通过验证的实现代码，只修复状态文档一致性；此前同一实现的完整 `scripts/ci.sh` real-SQLite/Native/frozen-runtime evidence 仍有效。worktree 仍未提交、未推送、未发布，hosted six-platform Release Matrix 未重新执行。

## 05:32 — Phase 12 FFI and acceptance re-review closure

Scope and authorization: 用户再次要求持续执行 Phase 12 code review → 修复 → re-review，直到当前范围没有剩余问题；继续允许代码、测试和文档修复，未授权 commit、push、发布或部署。

### Findings and changes

- 发现 FTS5 API lookup 的 output pointer 通过共享引用地址传给 SQLite 写入。改为真实 mutable output slot，避免 Rust aliasing contract 与外部写入冲突。
- 发现 constructor C string helper 以安全函数返回任意借用生命周期。改为 `unsafe` helper，并在 callback 内立即复制为 owned `String`，把 FFI 生命周期限制在实际 callback contract 内。
- 现有 synthetic probe 虽已覆盖 AUX flags、offset、callback error 与 document-side colocated synonym，但没有端到端证明 query-time override 的复杂参数与 colocated synonym。扩展 `lithograph-phase12` probe，新增 query-time complex args、QUERY/PREFIX child lifetime 平衡和 query-side synonym 命中断言。
- review 期间曾怀疑 tokenizer specification 空白分隔规则过窄；用真实 SQLite 对空格、tab/newline/CR/form-feed/vertical-tab 做差分 probe 后确认宿主行为与现有 parser 一致，因此该疑点关闭，没有扩大语法。

### Verification

- `cargo test --locked -p lithograph-fts5`：4/4 passed；`cargo test --locked -p lithograph-core --test phase12_fulltext_tokenizer`：11/11 passed；`cargo test --locked -p lithograph-core --test phase08_search_ingestion`：27/27 passed。
- 更新后的真实 `lithograph-phase12` probe 在 SQLite 3.53.4 上通过；随后最低 SQLite 3.45.0 与冻结 SQLite 3.53.4 smoke 都再次执行同一 Phase 12 synthetic tokenizer probe 并通过。
- `cargo make quality`：最终 exit 0；production duplicated lines 0.99%；coverage regions/functions/lines 83.26% / 84.35% / 85.26%；Clippy、Rustdoc、Lizard、machete、deny 与 coverage gate 均通过。
- 原始 `scripts/ci.sh` 在修复后的当前 worktree 重新完整执行并 exit 0；host SQLite 3.51.0、SQLite 3.45.0/3.53.4 real `.load`、Phase probes、Phase 12 dual-extension load orders、artifact inspection、Native ABI smoke、compat self-check/inventory/tck-inventory 全部完成。
- executable inherited openCypher TCK 仍为 3,777/3,777 applicable passed、0 failed；120 个 non-applicable scenario 的 machine-readable reason 未变化。

### Remaining

本轮 re-review 后，Phase 12 reviewed scope 内没有剩余 task-affecting finding。worktree 仍未 commit、未 push、未发布；hosted six-platform Release Matrix 本轮未重新执行。

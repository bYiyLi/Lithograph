# 2026-09-21

Timezone: UTC

## 09:36 — Phase 15 release delivery

- Continued the authorized Phase 15 implementation/review loop through a new formal release. The user explicitly authorized the remaining commit, push and publication work needed to make Phase 15 `done`.
- Local acceptance EX15-01–17 and EX15-19 is already closed: SQL-only application execution, true `lithograph_rows()` streaming, normal-surface explicit transactions, application Native query ABI removal, current storage format 3, Provider-owned persistent embedding cache, resource evidence, quality/CI and documentation review all have current local evidence.
- Chose v0.3.0 as the release version because Phase 15 removes public v0.2.x application query/transaction ABI and changes the current storage contract; the workspace package version and internal exact dependency constraints were updated from 0.2.1 to 0.3.0 and `docs/releases/v0.3.0.md` was added.
- `cargo check --workspace --all-targets --all-features` passed after the version change; all six workspace package entries in `Cargo.lock` resolve to 0.3.0 and `git diff --check` remains clean.
- EX15-18 is still pending until the committed/pushed revision passes the hosted Linux/macOS/Windows x64/arm64 Release Matrix. Phase 15 therefore remains `in_progress` at this checkpoint; the exact commit and hosted run identifiers will be recorded after remote verification.

## 09:58 — Phase 15 hosted interoperability fixture fix

- Pushed the first v0.3.0 candidate revision `0feb3423d072b33226041c0d88b570662a621048`. Repository CI `35584622281` passed, but Release Matrix `35584622310` failed all six Linux/macOS/Windows x64/arm64 artifact jobs at the final cross-platform storage-fixture verification.
- All six failures had the same cause and occurred after the SQL surface, explicit transaction, Provider and Managed Semantic artifact smokes had already passed: the Phase 10 fixture generator still created the two historical `semantic.embedding_cache.*` metadata columns while writing current `storage_format=3`. The current extension correctly rejected that physical schema as `LITHOGRAPH_STORAGE_ERROR: Lithograph metadata table schema does not match storage format 1`.
- Removed the historical cache-policy columns from the interoperability fixture and added an explicit fixture self-check requiring the current canonical four metadata columns: `id`, `magic`, `database_id`, `storage_format`.
- Targeted verification passed: fixture binary compile, test-support Clippy with `-D warnings`, fresh format-3 fixture creation, and real v0.3.0 release-extension reopen/verification under SQLite 3.51.0. The first local reopen attempt used PATH SQLite 3.41.2 and correctly could not initialize Lithograph because it is below the 3.45.0 minimum; rerunning with the repository's Homebrew SQLite 3.51.0 passed.
- EX15-18 remains pending until the fix revision passes a new hosted six-target Release Matrix.

# 2026-09-18

Timezone: UTC

## 01:24 — OpenAICompatible Provider design closure

Scope and authorization: 用户要求先停止代码实现，只整理并写入 OpenAICompatible 向量 Provider 的完整设计与 Phase 13 开发计划，并持续执行 review → 修复 → re-review，直到本次文档范围没有剩余 finding；未授权继续修改实现代码、commit、push、发布或部署。

### Decisions

- OpenAICompatible 作为独立 SQLite loadable extension / `EmbeddingProviderV1` reference implementation，注册名固定 `openai-compatible`，不依赖 `lithograph-core`，同一 connection 的一个 registration 可以服务多个不同 endpoint/config。
- 所有 Provider 配置统一进入 versioned `providerConfig`，不再有 extension-load 环境变量作为隐藏配置来源；完整字段为 `base_url`、`api_key`、`api_key_env`、`model`、`send_dimensions`、`encoding_format`、`user`、`organization`、`project`、`headers`、`timeout_ms`、`max_retries`、`batch_size`、`semantic_identity`。`dimensions` 继续由 Semantic Index 顶层 contract 拥有，`input` 来自每次 exact String batch。
- Lithograph 不过滤 secret：显式 `api_key` 或 secret custom header 会按原值进入 versioned Schema/history/SHOW/backup；`api_key_env` 只保存环境变量名，request-time 读取实际 value。
- Auth precedence 固定为 custom `Authorization` > `api_key` > `api_key_env` > no Authorization；organization/project 映射 OpenAI headers，custom headers 最后按大小写不敏感语义覆盖 generated headers。
- 支持 OpenAI Embeddings 当前 `float` / `base64` encoding、可选 dimensions request、`user`、Bearer、organization/project 与 custom headers；Managed Semantic 只接受 String source，因此不增加 token-array input surface。
- `send_dimensions=false` 用于不接受 OpenAI optional dimensions field 的模型；无论是否发送 request field，response 都必须匹配 Semantic Index 顶层 dimensions。
- Batch 只按 item count（1–2048）分批，不引入 tokenizer、不自动 truncate/chunk；remote endpoint 负责模型/token-limit error。Provider 支持 bounded timeout/retry/Retry-After/cancel，并严格校验 response count/index/dimension/finite values。
- Cache v1 保守使用完整 canonical `providerConfig` + implementation ABI semantic identity + dimensions；任何 config 变化都可以分裂 cache space。`semantic_identity` 是调用方用于 endpoint/model mapping 漂移的显式 versioned salt；同名 `api_key_env` 的 secret value 若改变语义路由，调用方也必须改变该 salt。

### Changes

- 更新 `docs/design.md` 的 Managed Semantic Provider contract、OpenAICompatible 完整 config/request/response/auth/cache/security 语义、示例与当前 Phase 13 实现状态。
- 更新 `docs/development/phases/13-managed-semantic-vector.md`，将 OpenAICompatible 作为 Phase 13.1 的正式 production/reference Provider，并把 acceptance 扩展到 SV13-35。
- 同步 `docs/research/embedding-provider-contract.md` 的 OpenAI Embeddings/Auth 证据与采用边界，移除旧的“只 runtime env / 只 float”描述。
- 保留当前未提交实现代码原状；本任务没有继续修改 Provider/Core/CI 实现。

### Verification

Source state and environment: `main` 比 `origin/main` ahead 1，HEAD `1e6e078`; worktree 在本任务开始前已经存在未提交的 Phase 13.1 Provider/CI 实现，本次只写文档。

Commands, scope, and observed results:

- 重新核验 OpenAI 官方当前 Embeddings API：`input/model/dimensions/encoding_format/user`、8192-token 单 input、300000-token 单 request、2048 input-array limit，以及 response `data[].index/embedding`。
- 重新核验 OpenAI 官方 Authentication/Request Headers：Bearer、`OpenAI-Organization`、`OpenAI-Project`、custom header budget 与 `X-Client-Request-Id` 可传递能力。
- 多轮 review 修正旧 runtime-env-only 设计、SHOW/security secret 语义、optional dimensions、multi-config registration、auth/header precedence、case-insensitive header collision、cache identity/default canonicalization、Phase 13 当前实现状态与 secret-safe diagnostics。
- `git diff --check`：通过。
- `docs/design.md`、Phase 13、Embedding Provider research 的 relative link/anchor 与 trailing-whitespace 检查：0 finding。
- OpenAICompatible 14 个配置字段在 Design 与 Phase acceptance 的覆盖检查：0 missing。

### Remaining

文档设计与 Phase 13 计划已闭合本次 reviewed scope；当前未提交实现仍是此前开始的 partial 13.1 work，尚未按新完整配置设计继续修正或验收。未执行 commit、push、发布或部署。

## 01:57 — 设计文档按责任域拆分

Scope and authorization: 用户要求按已讨论方案拆分设计稿，并在拆分后 review、修复遗漏与引用问题。本次只修改设计文档、受影响的文档导航/仓库规范和本记录；未授权或执行 commit、push、发布及实现代码修改。

### Decisions

- 保留 [设计入口](../design.md)，由入口与十个责任域专题共同构成唯一设计真源；专题声明合同归属并链接必要依赖，不按 Phase 或 crate 分裂设计。
- 入口保留产品定义、全局边界和设计驱动。详细合同使用稳定主题锚点；开发计划与发布记录分别拥有实施/验收状态和已发布能力，不把目标 format 4 / Managed Semantic 描述为已交付。
- 原稿 cache 段落曾把 API key、timeout、retry 排除在 key 外，与同稿完整 canonical providerConfig 哈希规则矛盾。已按原稿 Semantic IndexDefinition、OpenAICompatible 及本日 01:24 决策修正；Provider 特有的 secret 间接引用规则仍由 [OpenAICompatible](../design/vector.md#openai-compatible-provider) 拥有，没有增加 Core 配置解释或新能力。

### Changes

- `docs/design.md` 从 2250 行缩为 104 行；新增 `docs/design/` 下 compatibility、interfaces、query-engine、storage、versioning、schema-and-indexes、full-text、vector、runtime、decisions 十个专题。
- 同步 `AGENTS.md`、全部 Phase 设计输入、开发路线/compatibility inventory，以及受影响的 guide/reference/research 引用；修正 Phase 02/04 中遗漏的旧章节号和性能研究 tie-aware recall 的错节引用。
- 清除永久设计标题中的 Phase 标记，保留性能数值门禁、D1–D14 决策标识和格式迁移合同；阶段进展转为链接，不复制实施状态。
- 历史日志保留当时内容与编号，只修复 2026-09-16 一处失效链接的目标地址，不改写其历史表述。

### Verification

Source state and environment: Yi Mac 上 `/Users/yi/Code/Lithograph`，检查时 HEAD `1931bf9`。开始时已有 Provider/CI 未提交实现；检查期间又观察到 Cargo.lock、extension manifest/registration 与 Provider 实现发生并行改动。本任务没有覆盖、回滚或验收这些实现变化。

Commands, scope, and observed results:

- 独立逐节正文对照：原稿 118/118 个标题对应的正文均已归位；1902 行正文在明确记录的措辞修正和链接迁移之外保持一致，无遗漏或重复归属。
- 原稿 47 个 fenced code blocks（含 C ABI、Cypher、JSON、DDL 与结构示例）逐字保留；性能目标表 8 行保留；D1–D14 标识完整。
- 全部 14 个 Phase 的状态、验收勾选与代码示例未被本次拆分改变。
- Markdown 相对路径/锚点、重复锚点、代码围栏与新增文件行尾空白检查通过；旧设计编号的残留检查只允许历史记录和明确的 Phase 内部章节引用。
- 重新读取设计入口、专题归属和调整段落，审核接口/事务、Versioning/Storage、Schema/Search、cache/format4 的跨文件边界；审核全部受影响文档的 diff。
- `git diff --check -- AGENTS.md docs` 通过；Git staging 未被本任务改变。临时迁移/核对脚本与原稿快照只保存在仓库外，没有引入文档工具依赖或提交临时产物。

### Remaining

本次文档拆分与引用迁移范围未识别剩余 finding。未运行 Rust/SQLite/CI 实现测试，文档检查不代表并行 Phase 13 实现验收；未执行 commit、push、发布或部署。

## 12:32 — Phase 13 Managed Semantic implementation and local acceptance closure

Scope and authorization: 用户要求继续完成 Phase 13 代码开发，并持续执行 review → 修复 → re-review 直到当前范围没有剩余问题。授权包含 Phase 13 所需代码、测试、CI/quality 接线与文档同步；未授权 commit、push、发布或部署。

### Decisions

- 保持 Raw Vector / Cypher 25 `SEARCH` 不变；Managed Semantic 使用独立 `SEMANTIC` Index kind 与 `db.index.semantic.*` procedure，不创建 Lithograph query dialect。
- `EmbeddingProviderV1` 是 connection-local SQLite client-data ABI；unsafe 只留在 Provider ABI / extension 边界，Core 继续 `forbid(unsafe_code)`。OpenAI-compatible Provider 作为独立 loadable extension，每个 Index 的 endpoint/model/auth/runtime config 都来自 versioned `providerConfig`。
- storage format 4 只增加 database-local derived Embedding Result Cache 与 operational cache policy；canonical Commit/Layer/Schema history 不把 embedding vector 作为 truth。普通 query miss 只进 TEMP/LRU；persistent write 只发生在明确 maintenance/rebuild boundary。
- Semantic definition 的 provider/config/source 作为单一 `index/<name>` slot 参与 Diff/Patch/Merge/Rebase/Revert；任何新增/改变 definition 的 publication 都在 Branch move 前验证目标 Provider，纯 SHOW/history/DROP 不要求 Provider 当前可用。
- 本地 acceptance 已闭合，但 Phase 13 不标 `done`：SV13-27 仍要求 Linux/macOS/Windows x64/arm64 六目标 hosted Release Matrix；当前 worktree 未提交且本任务未授权 push，不能伪造远程制品证据。

### Changes

- 新增 public `lithograph-embedding-provider` ABI crate、C header、独立 `lithograph-openai-compatible` Provider、synthetic Provider fixture 与 HTTP oracle。
- 新增 versioned Semantic IndexDefinition、create/query/cache/rebuild procedures、Graph View/history/adapter boundary，以及 format4 persistent embedding cache、query-local LRU、FIFO capacity、integrity/migration。
- 扩展 Patch/Merge/Rebase/Revert/Native publication validation、release packaging/artifact inspection、SQLite smoke、coverage/quality 与 Phase 13 integration/performance/concurrency harness。
- 补齐 Semantic Diff/Patch/Merge/Rebase 直接 regression，以及 persistent cache 已 warm 后 Provider 缺失时 query/rebuild 仍 fail closed 的回归。
- 同步 README、CHANGELOG、guide/reference、compatibility、Phase 13 状态，并新增 [Phase 13 performance evidence](../research/phase13-semantic-performance-evidence.md)。

### Verification

Source state: Yi Mac `/Users/yi/Code/Lithograph`，`main` 相对 `origin/main` ahead 4；Phase 13 worktree 仍未提交。

Observed results:

- `lithograph-phase13` real-SQLite integration：17 个直接 checks 通过，覆盖 provider lifecycle、multi-label/type、Schema negative、exact Unicode/source type、query/rebuild/cache、Graph View、history、Diff/Patch/Merge/Rebase/Revert publication、warm-cache provider-missing、failure atomicity、rows/read-only 与 OpenAI config round-trip。
- OpenAI-compatible unit tests 15/15；Embedding ABI tests 3/3；embedding cache tests 6/6；Phase 11 persistent-cache regression 10/10。
- SQLite 3.45.0 与 3.53.4 real-load smoke 均通过 Lithograph + synthetic/OpenAI Provider、format migration、Native ABI、Phase 13 concurrency/performance。
- quantitative fixture：64 owners / 16 unique source texts；cold query 16 provider inputs，same-connection warm/new-connection persistent-warm 均 0；cold rebuild 16 embeddings / 16 entries / 256 bytes，warm rebuild 16 cache hits / 0 provider inputs。
- 1000 ms synthetic Provider wait 下，3.45/3.53 concurrency probe writer wait 0–1 ms、writer hold 4–14 ms，non-autocommit provider calls = 0。
- `cargo make quality`：exit 0；coverage regions/functions/lines 83.32% / 84.14% / 85.11%，新增 production files 均通过 per-file line coverage ≥ 50%。
- `scripts/ci.sh`：exit 0；applicable inherited openCypher TCK 继续为 3,777/3,777。
- `git diff --check` 与 Phase 13 runner fmt/clippy/complexity checks 通过。

### Remaining

SV13-27 的 minimum/current SQLite runtime 已通过；六目标 hosted Release Matrix 仍需当前 Phase 13 worktree 被提交/推送后由 CI 实际构建、打包、加载和迁移。由于本任务未授权 commit/push，本轮不执行该动作，Phase 13 状态保持 `in_progress`。未执行 commit、push、发布或部署。

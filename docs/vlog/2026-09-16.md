# 2026-09-16

Timezone: UTC

## 08:48 — Phase 11 local implementation and acceptance closure

Scope and authorization: 用户持续要求按Lithograph当前Design、Phase 11计划与repository规范完成Performance Optimization，并持续执行review → 修复 → re-review直到当前可执行scope无剩余finding；授权代码、测试、benchmark harness与文档修改，未授权commit或push。

### Decisions

- 保持Phase 11 `in_progress`：当前worktree的本地实现、固定性能机、Search/Merge/mixed stress、repository quality/coverage与真实SQLite CI都已闭合，但六目标hosted Release Matrix必须针对当前commit/ref运行；改动尚未commit/push，因此不能借用Phase 10旧matrix结果或提前标`done`。
- Persistent Standard Index保持derived data身份：ordinary query不为检测离线tampering执行全generation checksum/count；显式integrity gate负责完整校验，query path只验证manifest/definition/encoding及实际访问异常。Canonical property recheck继续以Snapshot为correctness source。
- Range热点使用与persistent range secondary index一致的直接bounded access path，并与changed-owner/query-local overlay合并；不再通过TEMP UNION view扫描整个generation。
- Vector HNSW warm query改为按traversal point-load实际访问entry，避免每次反序列化百万entry；level-0连接容量采用`2M`。FLOAT32 cosine exact cutoff存在大量同分项，recall验收采用Design冻结的tie-aware cutoff语义，并继续验证score、去重与稳定score/identity顺序。
- Query-owned resolved state/read guard、format3 migration、persistent generation、rebuild/GC/retention与staged/candidate overlay均保持canonical history不变；`lithograph.index.rebuild`不创建Commit或移动Branch。

### Changes

- 实现邻接复合keyset cursor、outgoing/incoming/incident统一分页及所有热点caller迁移。
- 实现connection-independent resolved Snapshot state、单query复用、read guard与same-connection mutation/GC boundary。
- storage format升级到3；新增persistent Standard Index generations/entries、`1 -> 2 -> 3` / `2 -> 3`迁移、legacy read/current-write boundary、`lithograph.index.rebuild`、retention与canonical GC清理。
- 实现ancestor persistent base + changed-owner delta、staged/Merge candidate query-local overlay、canonical property page prefetch与direct persistent numeric Range path。
- 增加Phase 11 Core/Search/delta runners以及SQL rows/Native adapter、streaming、mixed reader/writer C harness；Merge finalize增加prepare/writer-wait/writer-hold test-support instrumentation。
- Search HNSW cache改为lazy entry load；新增1M×128、100K×1536 Vector与1M Full-text corpus、cold/warm/historical/Graph View与tie-aware recall证据。
- 拆分热点production/support文件并修复complexity/file-budget/duplication findings；没有放宽quality threshold。
- 同步Design、Phase 11 plan、README/Development、research evidence；Cypher 25 compatibility Profile/waiver未变化，因此compatibility matrix正文保持不变。

### Verification

- 10M/100M：persistent equality 20样本P95 **2.209 ms**；1000-row bounded Range P95 **38.713 ms**；1000-owner delta old equality P95 **3.204 ms**、new 1000-row Range P95 **39.582 ms**；均`standardIndexBuilds=0`。
- Adapter：5个warm connection×20，另100 independent reopen；rows/Native adjacency warm P95约**1.767/1.588 ms**，indexed equality warm P95约**1.751/1.676 ms**；reopen indexed equality P95约**12.482/12.429 ms**，open/load另计并低于0.4 ms P95；Native explicit transaction零failure。
- Streaming：3轮10M Label scan SQL rows/Native median约**6.72/4.75 s**；1M Hub scan约**11.17/10.85 s**，零failure。
- Search：1M×128、100K×1536、1M FTS；所有Vector样本tie-aware recall@10=**1.0**。128维warm约14.9–75.3 ms，1536维warm约23.2–35.4 ms；warm query只加载约153–683个HNSW entries。Full-text warm/historical约0.63–5.14 ms。
- Merge：10,000 conflicts，40页/40轮；finalize约210 ms，其中prepare约195.429 ms、writer wait约42 µs、writer hold约2.050 ms；GC root preserved。
- Mixed stress：1/4/8 readers + 1 writer均各1800秒；reader/writer BUSY=0、failure=0，最终WAL=0。reader P95分别约103/171/477 ms；writer wait P95分别304/654/999 µs。
- `cargo make quality` **exit 0**；`cargo make quality-coverage` **exit 0**，TOTAL regions/functions/lines **83.26% / 84.41% / 85.28%**；applicable inherited openCypher TCK **3,777/3,777**。
- final `scripts/ci.sh` **exit 0**：真实SQLite `.load`、Phase 01–09 probes、artifact inspection、Native ABI smoke、compatibility harness/inventory与TCK inventory通过。`git diff --check`通过；worktree hygiene未发现database/report/trace/binary或secret混入Git候选。

### Remaining

- 当前Phase 11代码仍在dirty worktree，`HEAD == origin/main == da9445f2552d6b98de7f1275f69521e9099b7653`；本轮未commit、未push。
- 当前改动对应的Linux x64/arm64、macOS x64/arm64、Windows x64/arm64六目标hosted Release Matrix尚未运行；这是Phase 11唯一剩余acceptance，真实通过前状态保持`in_progress`。
- 旧26 GiB format2大fixture的完整integrity migration曾超过1小时测试窗口并安全回滚；Phase 11没有大库migration latency承诺，本轮不把它写成已优化能力。

## 10:21 — Phase 11 final review：planner复用resolved Schema

Scope and authorization: 继续按Phase 11与Design §7.8执行review → 修复 → re-review；本轮只追加scope内性能contract修复与验证，不执行commit/push。

### Finding and fix

- Phase-level review发现普通read已经把versioned `SchemaState`放入query-owned resolved state，但Standard Index seek选择仍按Commit再次从SQLite加载Schema；结果语义正确，但违背“Snapshot + Schema一次解析并在planner/executor共享”的§7.8性能合同。
- `select_standard_index_seeks`改为直接消费resolved state持有的`SchemaState`；current/historical与Merge candidate均沿用对应query state，不新增cache、配置或公共接口。

### Verification

- targeted exact regression通过：`planner_statistics_choose_most_selective_label_scan`、`historical_index_planning_uses_target_commit_type_proof`、`standard_indexes_cover_composite_range_text_spatial_and_relationship_seeks`。
- fresh `cargo make quality` **exit 0**；TOTAL regions/functions/lines **83.25% / 84.40% / 85.27%**，applicable openCypher TCK **3,777/3,777**；Clippy/Rustdoc/Lizard/jscpd/machete/deny均通过。
- fresh `scripts/ci.sh` **exit 0**；当前SQLite与最低SQLite 3.45.0真实`.load`、Phase 01–09 probes、format/migration/recovery、artifact inspection、Native ABI smoke及compatibility inventory通过。
- final `git diff --check`、新增TODO/FIXME扫描、untracked artifact/database检查与常见secret signature检查均通过；本轮review未发现新的scope内task-affecting finding。

### Remaining

- Phase 11仍为`in_progress`；唯一未满足acceptance仍是当前Phase 11 ref对应的六目标hosted Release Matrix。worktree尚未commit/push，因此不存在可供hosted runner验收的当前远端ref。

## 10:35 — Phase 11 current-tree performance smoke

- 为把最新planner Schema复用修复绑定到当前tree，补跑26 GiB固定规模fixture的`low_degree_one_hop`与`indexed_equality`结构性gate。
- 首次误用了`target/phase10-scale-release/scale.sqlite`：该fixture仍为storage format 2且不存在`_lithograph_index_generations`，因此`indexed_equality`按合同执行local fallback build并触发`standardIndexBuilds=1` gate失败；确认前置条件错误后未把该结果归因为实现regression。
- 改用`target/phase11-scale-prepared/scale.sqlite`（storage format 3、complete persistent generation）重跑通过：两个case均`resolvedStateBuilds=1`、`standardIndexBuilds=0`；low-degree adjacency为1页，indexed equality返回1行。本轮临时review report随后已删除，不进入Git候选。

## 11:53 — Phase 11 hosted Release Matrix closure

Scope and authorization: 用户明确授权提交并推送当前Phase 11改动，并运行六平台Release Matrix；若CI暴露scope内问题则继续修复直到acceptance闭合。

### Delivery and verification

- Phase 11实现提交为`4695ebbd5887b7fcafa2dd64d9886c4a066bfd05`（`perf: implement phase 11 performance optimization`），已推送到`origin/main`并确认远端ref指向该SHA。
- GitHub Release Matrix run `35091276800` 为`success`；storage fixture及Linux x64/arm64、macOS x64/arm64、Windows x64/arm64六个平台job全部完成且`success`。
- 六个平台artifact均已生成且未过期：`lithograph-linux-x64`、`lithograph-linux-arm64`、`lithograph-macos-x64`、`lithograph-macos-arm64`、`lithograph-windows-x64`、`lithograph-windows-arm64`。
- 同一实现提交的CI run `35091276893` 为`success`。本轮没有出现需要代码修复的hosted finding。

### Closure

- Phase 11最后一个外部门禁已满足，状态更新为`done`；同步Design、README、Development、Phase计划与performance evidence到真实状态。
- 旧26 GiB format2大库完整integrity migration超过1小时测试窗口的限制继续作为已知边界保留；Phase 11没有承诺该迁移的低延迟。
- 正式Release/发布未在本次授权范围内执行。

## 12:42 — v0.1.0 Release preparation

Scope and authorization: 用户确认首个正式版本使用 `v0.1.0`，并明确授权完成正式版本发布，包括仓库修改、commit/push、tag 与 GitHub Release publication。

### Release contract

- workspace/package version从开发占位的`0.0.0`冻结为`0.1.0`；内部path dependency同步使用`=0.1.0`。
- 正式二进制通过GitHub Release Assets分发，平台固定为Linux x64/arm64、macOS x64/arm64、Windows x64/arm64；asset名称保持稳定，便于`releases/latest/download/...`直接使用。
- 每个平台包包含extension binary、README、AGPL License、Commercial License说明与`VERSION`；Release额外生成`SHA256SUMS`。
- 现有Release Matrix继续拥有真实SQLite load/ABI/runtime/interop gate；只有`v*` tag且tag与Cargo版本一致时才上传正式distribution package，六个平台全部成功后才创建并发布GitHub Release。

### Changes and local verification

- 新增`CHANGELOG.md`、`docs/releases/v0.1.0.md`和`scripts/package-release.py`；README增加正式下载入口、版本/兼容性与升级边界。
- Release Matrix增加release metadata校验、Unix/Windows distributable packaging、六资产集合/checksum校验与draft→published GitHub Release步骤；普通branch/PR仍只运行验证，不产生正式Release。
- `scripts/package-release.py`通过Python compile、tar.gz/zip正向package probe与invalid-tag负向probe；包内容包含binary、README、LICENSE、COMMERCIAL-LICENSE.md、VERSION。
- workspace版本更新后`cargo metadata --locked`确认三个package均为`0.1.0`；`cargo check --locked --workspace --all-targets --all-features`、`cargo fmt --all -- --check`与`git diff --check`通过；workflow可被YAML parser读取并包含四个预期job。

### Remaining

- release-prep改动尚未commit/push；需要先让当前release-prep commit的CI与六平台Release Matrix通过，再创建并推送`v0.1.0` tag。tag run中的`publish-release`成功后才算正式发布完成。

## 16:02 — v0.1.0 Developer Documentation

Scope and authorization: 用户要求完整整理面向第三方应用开发者的 Lithograph v0.1.0 Developer Documentation。本轮授权文档、可执行示例与其验证；不修改引擎实现，不 commit/push，不移动 tag 或替换 Release Assets。

### Baseline and decisions

- 重新确认本地 `HEAD=19b5bd9`、tag `v0.1.0`，开始时 worktree 干净。GitHub API 确认 v0.1.0 已于 2026-09-16 13:08:03 UTC published，非 draft、非 prerelease；这更新了上面 release-preparation 记录之后的当前观察，不改写历史日志。
- 下载真正发布的 `lithograph-macos-arm64.tar.gz`，SHA-256 为 `71a50eb3d7b745dc5a616a12ad1c4bc06578b0ee17a4f7f7e1d4c0c4f7c0e278`。示例验证使用解压后的发布扩展，不用本地重新构建的引擎替代。
- 用户文档固定为 v0.1.0 / CY25-2026.08 / ABI 1 / format 3，描述真实使用接口；`docs/design.md` 继续是行为/技术设计真源，开发计划和 compatibility matrix 继续管理验收证据。没有为本次文档重新定义产品合同。

### Changes

- 新增 [Developer Documentation](../guide/README.md)：安装与校验、快速入门、Graph/Cypher、Schema/Index、Search/CSV、版本管理、Merge Session、事务并发、Graph View、Python/Native 集成、备份迁移维护、排障。
- 新增 [Reference](../reference/README.md)：SQL/Native API、execution options、JSON value encoding、错误、限制、profile、全部 function/procedure inventory 与已知问题。共 26 个新 Markdown 文件，另有 5 个示例/验证源码文件。
- 从实际扩展 `SHOW FUNCTIONS YIELD *` / `SHOW PROCEDURES YIELD *` 导出 172 条 function signature、33 个 procedure，提供可重复生成 Reference 的脚本。Procedure table 不采信错误的 returnDescription 类型 metadata。
- 添加 Python quickstart、持久 version/merge/backup workflow、POSIX Native 单 Commit transaction、指南 SQL/负向边界验证；使用内存库或自动清理的临时目录，不接触用户业务库。
- README 增加用户文档阅读路径、FTS5 前提、固定版本与 latest 区别，并链接 Known Issues。临时下载、二进制、inventory JSON 均留在忽略的 target 目录，不进入 Git 候选。

### Verification

- 发布制品校验、包内容检查、`lithograph_version()` / init：extension 0.1.0、ABI 1、CY25-2026.08、新库 format 3。
- `/opt/homebrew/bin/python3 -B docs/guide/examples/python_quickstart.py <released-extension>`：PASS；Python 3.14.0 / SQLite 3.51.0，创建、参数绑定、stream、Tag、更新、historical read、integrity。
- `version_workflow.py`：PASS；冲突、断开重连 Session、分批 resolution、stale revision 拒绝、candidate、finalize、Diff/Patch、Data null/clear、分页历史、Rebase/Squash/Reset/Revert、ref/GC、一致性 backup/reopen/integrity。
- `verify.py --sqlite3 <minimum/current-cli>`：9 篇指南、31 个 SQL 块、93 条 SQL，在 Python SQLite 3.51.0 与 CLI 3.45.0 / 3.53.4 均 PASS；负向验证覆盖只读 adapter/Snapshot、未知 options、Graph View、非法 structural params、transaction boundary、SQL rollback、CSV 和 introspection 差异。
- C 示例使用 `/opt/homebrew/opt/sqlite` 的 header/library，以 `-std=c11 -Wall -Wextra -Werror` 编译；两次 tx_execute 恰好一个新 Commit、events、callback cancellation、自动 abort、完整性检查 PASS。Native runtime SQLite 3.51.0。两个显式 probe 复现 checkout / NULL JSON 拒绝，按预期返回成功的验证退出码。
- Type encoding 另核对 Point、Vector canonical coordinateType、Date/Time/LocalDateTime/ZonedDateTime/Duration 的真实输出。校正文档中结构化图元素不能作为 params 传回的边界。
- 已逐文件读取并检查新文档/示例、Markdown fence 与末尾换行、Python compile、unfinished marker、临时文件混入；新文档与 README 的 180 个相对链接通过。修复一处错误性能证据路径，区分早期基线与最终 Phase 11 acceptance。
- 本轮不声称重新运行六平台 Release Matrix、完整 Rust CI/TCK 或大规模性能压测；当前直接运行的平台是 macOS arm64。

### Findings and remaining

- `DOC-V010-01`：SQL scalar / 普通 Native execute 的内部 savepoint 与 checkout 的 autocommit 检查冲突，使 `branch.checkout` 被拒绝。已在最低 SQLite 3.45.0 与当前绑定上复现；文档改用 query `options.branch` / tx_begin.branch。
- `DOC-V010-02`：Native `NULL,0` params/options 未自动默认成 `{}`，空 JSON 解析失败；tx_execute 错误会整体 abort。示例显式使用 `"{}",2`。
- `DOC-V010-03`：Procedure introspection 的 returnDescription.type 全部固定为 STRING，argumentDescription 为空，与真实 BOOLEAN/LIST/MAP/INTEGER 等返回值不符。Reference 保留准确签名/列名，单独描述实际类型，明确禁止据此生成类型绑定。
- 三项发布实现差异均保留复现、源码证据和规避方式，详见 [Known Issues](../reference/known-issues.md)；本轮没有修复引擎或宣称它们已经修复。
- 文档和示例已完成本地验证，仍未 commit、未 push，也未发布文档站点或新版本。

## 17:57 — Full-text tokenizer 设计与 Phase 12 计划

Scope and authorization: 用户要求把已讨论的全文扩展方向写入设计真源，建立 Phase 12，并持续 review/修正文档；允许 analyzer binding 破坏升级。本次只修改设计、研究、开发计划/状态、README 导航与日志，不修改引擎/测试代码，不 commit/push 或发布。

### Decisions

- 重新检查 `2cbca18`、clean worktree、AGENTS、Design、Phase 08/11、compatibility、Full-text 源码/测试和 quality/CI 命令；外部合同重新核对 SQLite FTS5 和 Neo4j Cypher 25 官方文档。证据见 [FTS5 tokenizer research](../research/fts5-tokenizer-contract.md)，行为唯一真源见 [Design §11.5](../design.md#115-full-text)。
- 对外保留现有 Cypher 配置字段，明确 FTS5 specification 属于 backend binding，不把字段相同解释为 Neo4j analyzer 算法/取值完全兼容。单一 versioned STRING 足够，不增加双模型、旧名称转换或无必要的 storage migration。
- 纳入实际 Schema 发布前的原生构造验证、历史配置/cache、connection/resource 生命周期、query-time analyzer 的正确原生委托，以及相关失败/rollback 验收。Vector/HNSW 和第三方分词算法不在范围。

### Changes

- 更新 `docs/design.md` 的 Profile binding 说明、Schema 验证边界及 §11.5.1–11.5.8；新增研究证据。
- 新增 [Phase 12](../development/phases/12-fulltext-tokenizer.md)：6 个有依赖顺序的 Feature、20 项 acceptance、真实 SQLite/ABI/compatibility/quality gate 和文档收尾要求。Phase 状态 `ready`，所有新验收均 `planned`。
- 同步开发路线、架构路线和 README 导航；Phase 00–11 历史完成记录保留。Compatibility 的 Full-text family 当前标 `partial`，明确由 Phase 12 修复源码已暴露的 query-as-document/词集合近似问题；新 provider inventory 单列，不以旧 green report 代替。

### Review closure

- 第一轮修正 Cypher/FTS5 兼容边界、legacy 名称与默认值、同名实现/词典不等于历史算法身份；补齐 constructor probe 与文本处理失败的不同保证。
- 第二轮拆开 DDL/query 示例，明确 literal quoting、旧连字符名称的 FTS5 内层引号、eventual metadata、pure ref move/no-op 与真正 Schema 发布的区别；消除 Feature 验收对后续 Feature 的循环依赖。
- 命令检查发现 `phase06_compatibility` 位于 `lithograph-test-support` 而不是 Core，已修正并重新检查实际 test target。补齐 query-time 非负分页参数和空结果不掩盖配置错误。
- 最终交叉 review 修正“当前 matrix 已无 partial”的过时总结：保留 Phase 10 当时结果，单独如实记录当前 Full-text 缺口。文档范围内没有剩余未裁决的配置/生命周期/验收 finding；这不表示引擎修复或 Phase 12 已完成。

### Verification

- 本轮文档验证（日志追加前）：7 个修改/新增 Markdown、73 个相对链接、4 个 fragment anchors 全部通过；UTF-8、fence、末尾换行、trailing whitespace、6 个 Feature、20 个 planned acceptance IDs、状态和既有 test/script 路径检查通过。`git diff --check` 通过；无代码/构建配置修改。
- 纯 SQLite 3.51.0 `:memory:` probe：5 个合法 tokenizer specification 示例、9 个预期失败/注入 sentinel 场景全部通过；额外验证绑定的 `alpha-beta` 查询命中。它们只验证 SQLite quoting/配置示例，不构成 Lithograph Phase 12 implementation evidence。
- 两个既有本地二进制的补充 query probe 报版本 `0.0.0` 并遇到 `LITHOGRAPH_BUSY`，未取得 override 运行结果；未把它们当作当前源码/发布制品的失败或通过证据。具体限制已记录在研究文档。
- 本次未重新编译或执行 Rust unit/integration、Clippy、完整 quality/CI、TCK 和六平台 release gate；这些是 Phase 12 实施后的验收，不是本次文档写入的完成声明。

### Remaining

设计与计划可进入实现；引擎目前仍有 analyzer 白名单和 query-time override 缺口，全部由 Phase 12 的未完成 acceptance 跟踪。本次没有 commit、push、新版本发布或第三方 Jieba 插件兼容性承诺。

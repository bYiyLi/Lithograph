# 2026-09-14

## 01:04 — Phase 09 final review closure

Scope and authorization: 用户继续要求按 Lithograph 仓库规范完成 Phase 09，并持续执行 review → 修复 → re-review，直到当前 Phase scope 无剩余 finding；本轮未授权 commit 或 push。

### Findings and changes

- 复现并修复 Rebase 维护第二套简化 three-way 导致的 correctness 缺口：Relationship endpoint dependency 与合并后派生 UNIQUE Constraint 现在复用 Merge 的 logical-slot、dependency 与 post-merge validation engine，并统一 conflictId/resolution semantics。
- 复现并修复 criss-cross DAG 中 Rebase 按 best-base Commit ID 任取一个 base、从而可能选择不在 active first-parent chain 上的 replay boundary；设计冻结为 active HEAD first-parent chain 上离 HEAD 最近且同时为 `onto` ancestor 的 Commit。
- 收紧 Rebase resolution：unknown conflictId、unknown field、`ours/theirs + value`、缺失 value 与 slot 类型不匹配均返回 `INVALID_ARGUMENT`，且 conflict rollback 不留下 partial rewritten history。
- 使用双 WAL connection + deterministic progress hook 复现 stale `merge.resolve` 从 read snapshot 升级 writer 时泄漏 `SQLITE_BUSY_SNAPSHOT`/`Storage` 的并发缺口；Version-only autocommit mutation 在完整 rollback 当前 savepoint 后仅对 primary `SQLITE_BUSY` 做一次 optimistic retry，stale caller 现在稳定返回 `MERGE_SESSION_CHANGED`，caller-owned outer transaction 不自动重试。
- Native explicit transaction state 从 process-global `HashMap<sqlite3*, state>` 迁移到 SQLite 3.44+ connection client data；通过 loadable-extension API table slots 获取 `sqlite3_get_clientdata` / `sqlite3_set_clientdata`，connection close 由 SQLite destructor 释放 state。最低支持 SQLite 3.45.0 真实 `.load`/smoke 已验证该路径。
- Patch input boundary 增加 `from/to` Commit descriptor、slot-target、before/after shape 校验；篡改 Patch 原子拒绝，且 Relationship + Property forward/inverse Patch round-trip 被独立回归锁定。
- 为 hard file budget 拆分 Phase 09 Merge/test 文件；没有放宽 file-budget、complexity、duplicate 或 coverage threshold。新增 Relationship Patch fixture 将 `query/version/patch.rs` 单文件 line coverage 从 49.69% 提升回 hard threshold 之上。

### Verification

- `phase09_version` **33/33**。
- `cargo make quality` **exit 0**：duplicated lines **0.96%**；coverage regions/functions/lines **81.41% / 83.40% / 83.59%**；hard per-file line coverage、format、strict Clippy、Rustdoc、production/support/C complexity、file budget、cargo-machete 与 cargo-deny 全部通过。
- final `scripts/ci.sh` **exit 0**：当前 SQLite 3.51.0 与最低支持 SQLite 3.45.0 real `.load`、Phase 01–09 probes、workspace/extension tests、artifact inspection、Native ABI smoke、compatibility harness/inventory 与 openCypher TCK inventory 全部通过。

### Remaining

当前 Phase 09 Versioned State、Merge/Rebase/Patch concurrency/correctness boundary、Native explicit transaction connection ownership、migration/restart 与 repository quality/CI scope 内未发现剩余 task-affecting finding。Phase 09 保持 `done`，Phase 10 保持 `ready`。本轮未 commit、未 push。

## 02:13 — Phase 09 follow-up Patch review closure

Scope and authorization: 继续按 Phase 09 当前设计与 Acceptance 执行 review → 修复 → re-review；本轮仍未授权 commit 或 push。

### Findings and changes

- 复现 canonical Patch 的同名 Index replacement correctness 缺口：`diff(A, B)` 原先会在同一个 `index/<name>` logical slot 上同时生成 `DropIndex` 与 `CreateIndex`，与“一 slot 最多一个 operation”的冻结合同冲突，导致 diff 可能生成自身无法 apply 的 Patch。设计与实现新增单一 `SetIndex` operation，forward/inverse round-trip 由独立 regression 锁定。
- review 发现 `AddLabel` / `RemoveLabel` typed `after` shape 缺口；准备修改时工作树已有并发修改写入等价修复。保留该并发改动并重新验证当前实现：Label operation 现在同时携带并校验 typed `before` / `after`。
- Phase 09 targeted regression inventory 已增长到 37 个；同步 Phase 09 completion evidence 与 Cypher 25 compatibility evidence，避免继续引用过时的 33/33。

### Verification

- `phase09_version` **37/37**。
- `cargo make quality` **exit 0**：coverage regions/functions/lines **81.49% / 83.50% / 83.67%**；format、strict Clippy、Rustdoc、complexity、duplicate threshold、file budget、cargo-machete、cargo-deny、coverage 与 Phase 01–09 probes 全部通过。
- final `scripts/ci.sh` **exit 0**：当前 SQLite 3.51.0 与最低支持 SQLite 3.45.0 real `.load`、workspace/extension tests、Phase 01–09 probes、artifact inspection、Native ABI smoke、compatibility harness/inventory 与 openCypher TCK inventory 全部通过。

### Remaining

本轮审查的 Phase 09 scope 内未发现剩余 task-affecting finding。Phase 09 保持 `done`，Phase 10 保持 `ready`。本轮未 commit、未 push。

## 02:15 — Phase 09 Rebase / SQLite ABI safety re-review

Scope and authorization: 用户继续要求 Phase 09 按规范执行 review → 修复 → re-review；本轮仍未授权 commit 或 push。

### Findings and changes

- 复现 Rebase fast-forward / no-replay 路径会在校验 `options.resolutions` 的 unknown conflictId 前移动 Branch 的缺口；现在无 replay 时先要求 resolution set 为空，再执行 ref move。另补 conflict-free replay 完成后才发现 unknown resolution 的回归，确认 outer savepoint 会回滚全部 rewritten Commit 与 Branch move。
- SQLite loadable-extension client-data state 使用 3.44+ append-only API slots。review 发现 init 原先在确认 host version 前就读取 268/269 slot；现在先通过 API table 的稳定 `libversion_number` prefix 验证 SQLite `>= 3.45.0`，不支持的 host 在访问新 slot 或初始化数据库 surface 前直接拒绝。3.45 baseline unit/real-load 均重新验证。
- canonical Patch format 继续收紧：`AddLabel` / `RemoveLabel` 现在生成并校验完整 typed `before/after`（`false -> true` / `true -> false`），并在 Patch round-trip fixture 中统一断言每个 operation 都包含 `before` 与 `after`。

### Verification

- latest `phase09_version` inventory **37/37**；workspace format + strict Clippy + extension all-features tests 均通过。
- fresh `cargo make quality` **exit 0**。
- fresh canonical `scripts/ci.sh` **exit 0**，覆盖当前 SQLite 3.51.0、最低 SQLite 3.45.0 real `.load`、Phase 01–09 probes、Native ABI、artifact 与 compatibility gates。

### Remaining

完成本轮静态 review 与 fresh repository gates 后，当前 Phase 09 scope 内未发现新的 task-affecting finding。Phase 09 保持 `done`，Phase 10 保持 `ready`；本轮未 commit、未 push。

## 02:53 — Phase 09 CI portability repair

Scope and authorization: Phase 09 提交 `7eece45` 推送后，GitHub Actions run `34774456853` 的 Linux canonical、quality 与 Windows jobs 失败；用户要求解决问题。按既有交付授权修复 CI portability defect，并在修复后重新执行本地门禁、提交、推送和远端 CI 验证。

### Findings and changes

- Linux `phase-01-gates` 与 `quality-gates` 共享的 Native ABI C smoke 在 strict C11 + `-Werror` 下使用 POSIX `usleep()`；glibc 默认不会在该语言模式暴露其声明，导致 implicit-declaration failure。改用 SQLite 公共 API `sqlite3_sleep(100)`，避免额外 feature-test macro 或平台分支。
- Windows Native ABI smoke 使用 MSVC `/std:c11 /W4 /WX`；Phase 09 clock regression 新增的两处 `strcpy` 会触发 C4996 并被 `/WX` 提升为编译错误。保留现有长度检查并改为按已验证长度执行 `memcpy(..., len + 1)`，不降低 warning policy。
- Phase 09 增加了 public Native transaction ABI，但 artifact gates 仍只校验旧 symbol 集。Unix/macOS artifact inspection 与 Windows `dumpbin` gate 现在同时要求 `lithograph_v1_tx_begin`、`lithograph_v1_tx_execute`、`lithograph_v1_tx_commit`、`lithograph_v1_tx_abort`。

### Verification

- current SQLite Native ABI smoke 与基于 repository SQLite 3.45.0 amalgamation 的 exact Native ABI smoke 均通过；artifact inspection 通过，测试源不再包含 `strcpy` / `strcat` / `sprintf` / `usleep`。
- `phase09_version` **37/37**。
- `cargo make quality` **exit 0**：coverage regions/functions/lines **81.49% / 83.50% / 83.67%**。
- canonical `scripts/ci.sh` **exit 0**：当前 SQLite 3.51.0、最低 SQLite 3.45.0、workspace/extension tests、Phase 01–09 probes、Native ABI、artifact、compatibility harness/inventory 与 openCypher TCK inventory 全部通过。

### Delivery state

本条记录写入时修复已完成并通过本地验证；后续 commit、push 与新 GitHub Actions run 状态在实际执行后单独确认。

## 06:24 — Phase 09 re-review closure and Phase 10 hardening start

Scope and authorization: 按当前 repository truth 重新对照 `docs/design.md`、Phase 09 Acceptance、实现、测试、Git state 与仓库规则执行 review → verify → re-review；确认 Phase 09 后继续进入 Phase 10。本轮未授权 commit 或 push，并保留工作树中已有相关修改。

### Phase 09 re-review

- 当前 Version Procedure 已锁定 query-start Commit/target head，并用 CAS 抵御并发 Branch move/delete；ref-only 与 Commit-producing version operation 的 `summary.commit` 行为按 Design 13.1 分离，组合 procedure 会以上一次 mutation 的结果继续 pin。
- Patch boundary 对 provenance、logical slot、typed before/after 与显式 identity 做 fail-closed 校验；forged 未分配 Node/Relationship identity 被原子拒绝。
- 重新审查 Merge Session、Rebase、Squash、Reset/Revert、GC、Commit Data/Tag 与 restart/migration boundaries 后，未发现剩余 task-affecting Phase 09 finding。当前 `phase09_version` inventory 为 **46/46**。

### Phase 10 progress

- 新增/整理 deterministic generated hardening：parser fail-closed、recursive Value JSON round-trip、Layer canonicalization/hash permutation、delta replay、public diff/patch forward+inverse algebra、checkpoint/Snapshot equivalence；`phase10_hardening` **6/6**。
- recovery fixture 同时锁定 SQLite writer transaction 的两侧合法状态：未 Commit 后 close/reopen 必须完整 rollback；Commit 后 close/reopen 必须保留 Commit/ref、Commit Data、Tag、checkpoint 且 integrity clean；`phase10_recovery` **2/2**。
- 既有 format `1 -> 2` migration / rollback / minimum-runtime probe 继续作为 Phase 10 migration foundation；未复制第二套 storage migration path。

### Verification

- targeted：`phase09_version` **46/46**、`phase10_hardening` **6/6**、`phase10_recovery` **2/2**，strict core test Clippy 通过。
- `cargo make quality` **exit 0**：coverage regions/functions/lines **81.72% / 83.65% / 83.91%**；format、workspace all-target/all-features Clippy、Rustdoc、complexity/duplication、dependency/security gates 与 coverage probes 全部通过。
- canonical `scripts/ci.sh` **exit 0**：当前 SQLite 3.51.0 与最低 SQLite 3.45.0 real `.load`、workspace/extension tests、Phase 01–09 probes、artifact inspection、Native ABI、compatibility harness 与完整 openCypher TCK inventory 读取均通过。

### Remaining

Phase 10 现为 `in_progress`。仍需完成 `CY25-2026.08` / applicable TCK compatibility closure（当前 EXPLAIN、PROFILE、error compatibility 等 release rows 尚未闭合）、完整 crash fault matrix 与 migration release matrix、10M/100M scale gate、cross-platform artifact matrix、final architecture/security review 及 documentation/release closure。本轮未 commit、未 push。

## 07:26 — Phase 10 compatibility / recovery / migration hardening

Scope and authorization: 继续从当前 repository truth 推进 Phase 10；先 fresh 回归 Phase 09，再处理 release compatibility、recovery 与 migration finding。本轮未授权 commit 或 push，并保留工作树既有修改。

### Findings and changes

- public error taxonomy 原先在 Core 将 SQLite `BUSY/LOCKED` 归为 generic `Storage`，只有 Extension adapter 根据 `sqlite_code` 二次改写为 `BUSY`。现在 Core `QueryErrorKind` 显式包含 `Busy`，`BUSY/LOCKED` 从 source mapping 起即稳定分类，并在 Extension 保留 primary SQLite code；Busy/Storage message 都继续做 public redaction，不暴露底层 SQLite storage detail。
- 新增 `phase10_compatibility`：锁定 mutating query 与 Version Procedure `EXPLAIN` 只 validation/planning、不写 graph/ref；`PROFILE` result 与 normal execution 一致并具备 aggregate rows/dbHits/time foundation；parse/semantic/type source position、constraint category 与 BUSY/LOCKED primary-code mapping 均有 release regression。当前 **3/3**。
- `phase10_recovery` 扩展 uncommitted sidecar/ref teardown fixture：Commit Data clear 与 Tag move/create/delete 在 transaction 未 Commit 就 connection teardown 后，reopen 必须完整恢复 transaction 前的 Data/Tag state 并保持 integrity clean。当前 recovery suite **3/3**。
- format `1 -> 2` real-extension migration fixture 从 frozen Root-only 提升为 Root + descendant history：迁移前后 Root/descendant Commit identity、两级 Snapshot、main head 与 parent DAG edge 均不变；原有 migration failure rollback 保持通过。

### Verification

- fresh `phase09_version` **46/46**，当前审查仍未发现 task-affecting Phase 09 finding。
- `phase10_compatibility` **3/3**、`phase10_hardening` **6/6**、`phase10_recovery` **3/3**。
- `lithograph-test-support --all-targets --no-run` 通过；使用 load-extension-capable SQLite 3.51.0 的 real `lithograph-phase09` probe 通过 strengthened format1->2 history migration、migration rollback 与 Merge Session restart/adapters。
- 本地系统 `/usr/bin/sqlite3` 虽报告 3.51.0，但 CLI `.load` 不可用；该手工 probe 失败不属于 Lithograph implementation failure。随后按 repository runtime contract 切换到 `/opt/homebrew/opt/sqlite/bin/sqlite3` 3.51.0 后真实 `.load` probe 通过。

### Remaining

Phase 10 继续 `in_progress`：PROFILE 仍缺 per-operator runtime counters 与最终 public-surface/oracle closure；EXPLAIN/error 仍需 applicable TCK/oracle + SQL Bridge/Native parity 收尾；完整 crash fault matrix、migration release matrix、10M/100M scale、cross-platform artifacts、final architecture/security/docs review 仍待闭环。本轮未 commit、未 push。

### Phase 10 public PROFILE metrics parity

- 复核 Phase 09 的 79 项 phase acceptance 均为 checked，并重新执行 `phase09_version`：46/46 通过；当前 review 未发现新的 task-affecting Phase 09 finding。
- Phase 10 review 发现 Core `QuerySummary.metrics` 已累计 rows/dbHits/elapsed time，但 SQL Bridge scalar summary 与 Native `SUMMARY` event 共用的 serializer 完全丢弃该字段，导致 PROFILE 的已有 runtime evidence 无法从 public adapter 观察，亦破坏 SQL/Native release parity 的闭环条件。
- 修复共享 `summary_json`，固定公开 `metrics.rows`、`metrics.dbHits`、`metrics.elapsedMicros`；SQL scalar 与 Native callback 因复用同一 serializer 自动保持同 shape。新增 extension regression 锁定该公开边界，并同步 `docs/design.md` Result Contract。
- 这只关闭 aggregate PROFILE metrics 的 adapter parity；per-operator runtime counters、最终 oracle/TCK closure 仍未完成，Phase 10 继续 `in_progress`。

### Phase 10 process-death recovery boundary

- 将原有 connection teardown recovery 提升到真实 subprocess death：子进程切换 WAL、开启 `BEGIN IMMEDIATE`，写入 Node allocation、Layer/Commit、Branch move、Commit Data、Tag、checkpoint 后在 SQLite COMMIT 前直接 `process::exit(86)`；父进程 reopen 后验证 Branch 回到 Root、无 descendant Commit/Tag、allocator state 完整回滚且 integrity clean。
- 对称增加 COMMIT 后立即 `process::exit(87)`：父进程 reopen 验证新 Commit/ref、Commit Data、Tag、checkpoint 全部 durable 且 integrity clean。该路径不会依赖 Rust `Drop` 对 SQLite connection 做正常 teardown，因此比原有 close/reopen fixture 更接近 crash-recovery boundary。
- `phase10_recovery` 当前 test binary **6/6** 通过；其中 `crash_writer_subprocess` 在普通 suite 中是 no-op harness，两个父测试分别校验 pre-commit rollback 与 post-commit durability。

## 08:15 — Phase 10 transactional fault-injection recovery

Scope and authorization: 从已提交并推送的 `0587cc0` clean `main` 继续按 repository truth 推进；先重新核对 Phase 09 Design/Acceptance/Git state 并执行 fresh regression，再扩展 Phase 10 crash/recovery fault matrix。本轮未授权 commit 或 push。

### Phase 09 re-review

- Phase 09 仍为 `done`；重新执行 `phase09_version` **46/46** 全通过，未发现新的 task-affecting finding，因此没有修改 Phase 09 implementation 或 acceptance record。

### Phase 10 findings and changes

- crash/recovery matrix 原先已有 process-death pre/post COMMIT 与 connection-teardown evidence，但缺少 canonical Commit write 内部故障的 deterministic rollback fixture。新增 Branch CAS fault：Layer/Commit/delta 已进入 storage savepoint 后，TEMP trigger 强制 Branch update 失败，验证新 canonical objects 与 delta 全部回滚、Branch 保持旧 head 且 integrity clean。
- Merge Session 的 Design 14.2 要求 operational workspace mutation 与 finalize canonical/ref action 保持事务原子性。新增 abort fault：session row 删除成功后 resolution cleanup 被 trigger 拒绝，query write savepoint 必须恢复 Session 与全部 resolution；新增 fast-forward finalize fault：Branch 已 move 后 Session delete 被 trigger 拒绝，必须回滚 ref move并保留 Session，避免出现“已 finalize 但 session 仍存活”的中间态。
- 上述 fault fixture 只在 disposable test database 使用 TEMP trigger，不改变 production storage schema，也不引入 runtime failpoint。

### Verification

- fresh Phase 09：`cargo test --locked -p lithograph-core --test phase09_version` **46/46**。
- Phase 10 recovery：`cargo test --locked -p lithograph-core --test phase10_recovery` **9/9**。

### Remaining

Phase 10 继续 `in_progress`。本轮关闭了 canonical Commit/ref、Merge abort 与 fast-forward finalize 的 deterministic transaction-fault evidence，但 Feature 10.3 仍需覆盖其余 explicit transaction、Commit Data/Tag 单操作边界、merge ready/finalize、checkpoint/index rebuild 与 migration fault points；Compatibility closure、migration release matrix、10M/100M scale、cross-platform artifacts、final architecture/security/docs review 也仍未完成。本轮未 commit、未 push。
